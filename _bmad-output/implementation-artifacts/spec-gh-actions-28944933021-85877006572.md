---
title: 'Fix GitHub Actions Aspire API route failures from run 28944933021 job 85877006572'
type: 'bugfix'
created: '2026-07-08'
status: 'in-review'
baseline_commit: 'b18b7dc47ec2ce1d2adee0917c4e8a34901cf21c'
context: []
---

<frozen-after-approval reason="human-owned intent - do not modify unless human renegotiates">

## Intent

**Problem:** The `ci / aspire-tests` job `85877006572` in GitHub Actions run `28944933021` fails 21 `Hexalith.Tenants.IntegrationTests` cases after restore/build succeeds. The API structural test finds no generated `Hexalith.Tenants.Api.Generated.TenantsRestController`, all generated API route tests receive `404 NotFound`, and three hosted UI smoke tests miss their unauthorized route state because the API route surface is absent.

**Approach:** Align the `Hexalith.Tenants.Api` RestApi generator dependency so the default CI sequence, `dotnet restore` followed by `dotnet build --configuration Release --no-restore`, restores the same generator input that Release build consumes. Preserve explicit source-reference debugging, but make the published generator package the default dependency path.

## Boundaries & Constraints

**Always:** Keep `.slnx` restore/build usage, warnings-as-errors, centralized package versions, and the external API boundary where generated controllers delegate through `IEventStoreGatewayClient`. Preserve the root-declared submodule policy and do not modify submodule contents.

**Ask First:** Halt before changing the shared `Hexalith.Builds` workflow, editing `references/Hexalith.EventStore`, removing the external API generated controller surface, bypassing integration tests, or changing route/authorization semantics.

**Never:** Do not hand-write replacement Tenants API controllers, convert the API host back to domain/UI host references, weaken authorization expectations to accept 404s, add inline package versions, or use recursive submodule commands.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| CI restore/build default | `dotnet restore Hexalith.Tenants.slnx` runs without explicit configuration, then `dotnet build Hexalith.Tenants.slnx --configuration Release --no-restore` | Release build receives the RestApi generator analyzer and emits `TenantsRestController` into `Hexalith.Tenants.Api` | Missing generator or missing controller fails structural tests before route smoke tests mask the root cause |
| Explicit source debugging | Developer opts into source refs with `UseHexalithProjectReferences=true` and the EventStore submodule is present | The API host may consume the local `Hexalith.EventStore.RestApi.Generators.csproj` as an analyzer project reference | If the source generator path is unavailable, default package mode remains the safe fallback |
| API route runtime | Generated query/command routes are requested in integration tests | Routes authorize, validate, forward to `IEventStoreGatewayClient`, and return the expected status codes instead of `404 NotFound` | Gateway failures still map to support-safe problem details; auth failures remain `401`/`403` |

</frozen-after-approval>

## Code Map

- `src/Hexalith.Tenants.Api/Hexalith.Tenants.Api.csproj` -- owns the conditional generator dependency that currently diverges between default restore and Release build.
- `tests/Hexalith.Tenants.IntegrationTests/TenantsApiStructuralTests.cs` -- structural guard for the external API host project, generated controller type, route attributes, and gateway-only boundary.
- `.github/workflows/ci.yml` -- passes Tenants inputs into the shared Hexalith domain CI workflow; useful context only unless Tenants-specific inputs must change.
- `references/Hexalith.Builds/.github/workflows/domain-ci.yml` -- evidence that shared CI restores without configuration and then builds/tests Release with `--no-restore`; do not edit for this fix.

## Tasks & Acceptance

**Execution:**
- [x] `src/Hexalith.Tenants.Api/Hexalith.Tenants.Api.csproj` -- change the local RestApi generator project-reference selector so default restore and Release build both use the published `Hexalith.EventStore.RestApi.Generators` package, while explicit source-reference mode still uses the local analyzer project -- removes the CI restore/build graph mismatch that drops generated controllers.
- [x] `tests/Hexalith.Tenants.IntegrationTests/TenantsApiStructuralTests.cs` -- add or update a focused structural assertion covering the generator dependency conditions for default package mode and explicit source-reference mode -- catches future graph drift close to the API boundary.

**Acceptance Criteria:**
- Given the default CI restore/build sequence, when `Hexalith.Tenants.Api` is built in Release with `--no-restore`, then `Hexalith.Tenants.Api.Generated.TenantsRestController` is present in the assembly.
- Given generated API route tests request `/api/tenants`, `/api/tenants/{tenantId}`, `/api/users/{userId}/tenants`, and command routes, when the gateway test double is configured, then responses match existing authorization, validation, query, command, and problem-detail expectations instead of `404`.
- Given a developer explicitly sets `UseHexalithProjectReferences=true`, when the EventStore submodule is available, then the generator can still be consumed from the local project as an analyzer without modifying submodule files.

## Spec Change Log

## Verification

**Commands:**
- `dotnet restore Hexalith.Tenants.slnx` -- expected: restore succeeds in the same shape CI uses, without requiring `Configuration=Release`.
- `dotnet build Hexalith.Tenants.slnx --configuration Release --no-restore` -- expected: Release build succeeds with generated Tenants API controller output.
- `dotnet test tests/Hexalith.Tenants.IntegrationTests/Hexalith.Tenants.IntegrationTests.csproj --configuration Release --no-build --filter "FullyQualifiedName~TenantsApiStructuralTests|FullyQualifiedName~TenantsApiGeneratedControllerTests"` -- expected: API structural and generated-route tests pass.
- `dotnet msbuild src/Hexalith.Tenants.Api/Hexalith.Tenants.Api.csproj -getItem:PackageReference -getItem:ProjectReference` and the same command with `-p:UseHexalithProjectReferences=true` -- expected: default mode lists the generator package path, explicit source-reference mode lists the generator project reference as an analyzer.
