---
title: 'Fix CI package governance tests after workflow standards alignment'
type: 'bugfix'
created: '2026-07-09'
status: 'done'
baseline_commit: 'd7e8cfef5fb650f632ff2055dae7a25acc41e993'
context:
  - references/Hexalith.AI.Tools/hexalith-llm-instructions.md
  - _bmad-output/project-context.md
  - _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-09.md
---

<frozen-after-approval reason="human-owned intent - do not modify unless human renegotiates">

## Intent

**Problem:** GitHub Actions run `29006773787`, job `86080137949` failed in `ci / build-and-test` during Tier 1 `Hexalith.Tenants.Contracts.Tests` because `PackageGovernanceTests` still assert the pre-standards-alignment workflow shape. The same four failures reproduce locally: expected explicit `dapr-version`, forbidden `StyleCop` text in `Directory.Build.props`, forbidden `Hexalith.Tenants.Aspire` text in a comment, and a missing deleted `release.config.cjs`.

**Approach:** Update the Tenants governance tests so they verify the current, intentional CI/CD standards alignment recorded on 2026-07-09: shared default Dapr version, `.releaserc.json` as the only semantic-release config, explicit release secret mapping, workflow-run release gating, and documented exclusion of the Aspire package from the package-only consumer harness.

## Boundaries & Constraints

**Always:** Keep this a governance-test drift fix unless implementation finds production CI behavior that contradicts `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-09.md`. Preserve assertions that local workflows delegate to `Hexalith.Builds` reusable workflows at `@main`, use `Hexalith.Tenants.slnx`, run package consumer validation, enforce coverage gates, publish containers, validate release secrets before NuGet push, and avoid recursive submodule operations.

**Ask First:** Halt before changing `.github/workflows/*.yml`, `.releaserc.json`, release scripts, package validation scripts, `Directory.Build.props`, the `references/Hexalith.Builds` submodule, package publish scope, release trigger semantics, or repository secrets. Halt if the only way to make tests pass is to weaken CI/release governance rather than align assertions with the documented policy.

**Never:** Do not restore `release.config.cjs`, re-add redundant `dapr-version` inputs, use `secrets: inherit`, reintroduce release test reruns, remove release secret preflight checks, remove the Aspire package from the published package set, or hide analyzer-package imports by plain substring tricks.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| CI workflow delegates to shared defaults | `.github/workflows/ci.yml` omits `dapr-version` and includes `coverage-line-scope` | Governance test passes while still proving build, test, consumer validation, coverage, and Aspire test inputs are present | If explicit Dapr version returns, test should not require it; shared default owns that value |
| Release workflow is CI-gated | `.github/workflows/release.yml` uses `workflow_run`, push-event success guard, explicit secrets, and no test rerun inputs | Governance test passes and verifies the release cannot publish from failed or scheduled CI | If `secrets: inherit` or push-trigger release returns, test fails |
| Live semantic-release config is single-source | `.releaserc.json` contains prepare/publish commands and `release.config.cjs` is absent | Governance test reads only `.releaserc.json` and still checks package validation, NuGet push, container publish, and secret preflight order | If a second release config is reintroduced, governance should fail or require explicit review |
| Governance comments mention forbidden names | `Directory.Build.props` documents that StyleCop/Sonar/Roslynator are intentionally not imported, and consumer validator comments explain Aspire exclusion | Tests distinguish comments/documentation from executable package references or consumer harness package ids | If analyzer packages are actually imported or Aspire enters package-only consumer ids, tests fail |

</frozen-after-approval>

## Code Map

- `tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs` -- stale governance assertions causing all four Tier 1 failures; should be updated to assert the documented July 9 workflow/release/script shape.
- `.github/workflows/ci.yml` -- source of truth for current CI caller; intentionally omits redundant `dapr-version` and includes `coverage-line-scope`.
- `.github/workflows/release.yml` -- source of truth for current release caller; intentionally uses `workflow_run`, explicit secrets, and no test rerun inputs.
- `.releaserc.json` -- only active semantic-release config after `release.config.cjs` deletion; owns prepare and publish command assertions.
- `Directory.Build.props` -- documents analyzer-policy exception; tests should detect real analyzer package imports without banning explanatory text.
- `scripts/validate-consumer-package-references.py` -- package-only consumer harness; intentionally documents why `Hexalith.Tenants.Aspire` is not part of `PACKAGE_IDS`.
- `scripts/validate-release-secrets.sh` -- release preflight still required before any irreversible publish command.

## Tasks & Acceptance

**Execution:**
- [x] `tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs` -- update CI workflow assertions to remove the redundant `dapr-version` expectation and require `coverage-line-scope` plus existing build/test/coverage/submodule safeguards.
- [x] `tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs` -- update release workflow assertions to reflect `workflow_run`, push-event success guard, explicit secret mapping, no `secrets: inherit`, and no release test rerun inputs.
- [x] `tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs` -- remove the dependency on deleted `release.config.cjs`; assert `.releaserc.json` contains prepare validation, release secret preflight, NuGet push, container publish, expected package artifacts, and the correct order.
- [x] `tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs` -- replace broad forbidden-substring checks with structure-aware checks for analyzer package imports and consumer harness `PACKAGE_IDS` membership, preserving support-safe explanatory comments.

**Acceptance Criteria:**
- Given the current July 9 CI workflow, when `PackageGovernanceTests` run, then CI governance passes without requiring a redundant `dapr-version` input.
- Given the current release workflow and `.releaserc.json`, when release governance runs, then it verifies CI-gated release, explicit secrets, package validation, secret preflight, NuGet push, and container publishing without reading `release.config.cjs`.
- Given comments mention analyzer package names or `Hexalith.Tenants.Aspire`, when governance tests inspect the files, then comments are allowed but executable package references and package-only consumer ids remain blocked.

## Spec Change Log

## Verification

**Commands:**
- `dotnet test tests/Hexalith.Tenants.Contracts.Tests/Hexalith.Tenants.Contracts.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~PackageGovernanceTests` -- expected: all package governance tests pass.
- `dotnet test tests/Hexalith.Tenants.Contracts.Tests/Hexalith.Tenants.Contracts.Tests.csproj --configuration Release --no-restore` -- expected: full Contracts test project passes.
- `dotnet build Hexalith.Tenants.slnx --configuration Release --no-restore` -- expected: Release build succeeds with zero warnings/errors.

## Suggested Review Order

**Release Governance**

- Start here: release gating, explicit secrets, and single config source.
  [`PackageGovernanceTests.cs:286`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L286)

- Publish ordering keeps secret preflight before NuGet and containers.
  [`PackageGovernanceTests.cs:320`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L320)

**CI Coverage Inputs**

- CI assertions target reusable-workflow inputs instead of loose comments.
  [`PackageGovernanceTests.cs:240`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L240)

- Coverage line scope is verified inside the literal input block.
  [`PackageGovernanceTests.cs:261`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L261)

**Parser Guardrails**

- Analyzer checks scan actual package-reference nodes only.
  [`PackageGovernanceTests.cs:523`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L523)

- Python collection parsing supports list and frozenset shapes.
  [`PackageGovernanceTests.cs:561`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L561)

- YAML helpers scope assertions to the intended job blocks.
  [`PackageGovernanceTests.cs:595`](../../tests/Hexalith.Tenants.Contracts.Tests/PackageGovernanceTests.cs#L595)
