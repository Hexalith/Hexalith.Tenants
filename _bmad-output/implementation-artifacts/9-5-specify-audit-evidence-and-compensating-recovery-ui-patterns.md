---
baseline_commit: cc442399689898c3aea2b3f89f7562396da2177d
---

# Story 9.5: Specify Audit Evidence and Compensating Recovery UI Patterns

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a tenant auditor or operator,
I want audit evidence and recovery patterns to be explicit in the UI plan,
so that access changes can be proven later and corrections are handled as auditable commands.

## Acceptance Criteria

1. **Given** audit entry points are specified, **when** users navigate from global navigation, tenant rows, tenant detail, user lookup, or command results, **then** each path can lead to audit context, **and** missing audit capability is represented as a documented fallback or blocked dependency.
2. **Given** an audit timeline dependency is unavailable, **when** the first UI slice still needs audit visibility, **then** the approved fallback is a flat audit DataGrid with stable ordering, filters, loading, empty, error, and accessible expansion states, **and** reusable timeline dependency status remains visible in the dependency map.
3. **Given** Audit Evidence Receipt is specified, **when** meaningful access changes complete or partially complete, **then** the receipt includes actor, target, tenant scope, outcome, timestamp, projection marker, audit reference, and support-safe command reference where available, **and** copyable references do not expose raw payloads, bearer tokens, stack traces, or sensitive internals.
4. **Given** audit proof is delayed or unavailable, **when** the UI communicates outcome, **then** it distinguishes audit pending, audit delayed, audit unavailable, and missing implementation support, **and** it avoids success language when proof cannot be verified.
5. **Given** recovery guidance is specified, **when** a wrong access change is discovered, **then** UI language uses explicit compensating-command terms such as start correction or restore intended access, **and** recovery is never labeled as hidden undo.

## Tasks / Subtasks

- [x] Preserve the Epic 9 planning-only boundary before editing any artifact. (AC: 1-5)
  - [x] Confirm this story produces planning/specification documentation only — an **audit-evidence and compensating-recovery UI pattern specification** for Phase 2 UI implementation stories. It does NOT make any audit screen, receipt, or recovery flow implementation-ready.
  - [x] Do NOT implement Tenants Admin UI screens, FrontComposer/Fluent UI components (Audit Evidence Receipt, Flat Audit List Fallback, Truth State Badge, Command Lifecycle Panel, Consequence Preview, etc.), Blazor pages/routes, backend endpoints, commands, queries, package references, generated UI files, domain-contract annotations, or submodule pointer changes. [Source: `_bmad-output/planning-artifacts/epics.md#Epic 9: Administrators Can Plan Phase 2 UI Access Operations Safely`]
  - [x] Keep backend MVP work independent from UI dependency readiness; missing UI dependencies block or defer future Phase 2 UI rows, never backend package/release work. [Source: `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`]
- [x] Reconcile with existing Epic 9 planning artifacts instead of duplicating them. (AC: 1-5)
  - [x] Read `docs/tenants-ui-truth-state-and-action-availability-spec.md` (Story 9.3, canonical owner of the truth-state badge vocabulary, the layered feedback states including the audit-evidence states `audit pending` / `audit available` / `delayed` / `unavailable` / `approved fallback`, the `missing audit proof → FC-AUD` reason mapping (§4.4), the non-collapse invariant (§5.2), the concurrency/recovery table (§5.4), and the "never label recovery as undo" rule (§5.4)). This spec **composes** those patterns; it must NOT redefine badge states, feedback states, reason categories, or the non-collapse invariant.
  - [x] Read `docs/tenants-ui-operations-shell-spec.md` (Story 9.2) §1.2/§6 for the audit **entry points** (global nav, tenant rows, tenant detail, user lookup, command result) and the rule that audit is never the only way to reach proof but is always reachable from any access-review context. Preserve tenant/user scope across entry points; do not promote audit or command lifecycle into a separate primary navigation model. [Source: `docs/tenants-ui-operations-shell-spec.md#6. Audit Entry-Point Context`]
  - [x] Read `docs/tenants-ui-frontcomposer-dependency-map.md` (Dependency ID Catalog + Audit Trail screen row + §"Audit fallback") and reuse its dependency IDs verbatim — do NOT redefine `FC-TBL`, `FC-LYT`, `FC-CMD`, `FC-CNC`, `FC-AUD`, `FC-CNS`, `FC-TOK`, `FC-A11Y`, `FC-L10N`, `FC-DOC`. The audit/recovery patterns map primarily to `FC-AUD` (audit timeline, `missing`), `FC-TOK` (timeline-connector/status tokens, `missing` for timeline), `FC-A11Y`, `FC-L10N`, `FC-LYT`, `FC-DOC` (and `FC-CMD` where the Audit Evidence Receipt is produced from a command lifecycle outcome).
  - [x] Read `docs/tenants-ui-remove-user-from-tenant-journey-spec.md` (Story 9.4) — it references audit expectation and recovery path and **explicitly defers** the full Audit Evidence Receipt, flat audit DataGrid fallback, and compensating-recovery UI patterns to this story (9.5). This story supplies those deferred patterns; keep them consistent with the 9.4 journey (e.g. the `audit_pending`/`audit_available` states and the support-safe command reference).
  - [x] Read `docs/tenants-ui-phase-2-story-backlog.md` and bind the audit-surface patterns to the consuming rows `ui-11-audit-trail-flat-timeline` and `ui-12-tenant-detail-audit-tab`. Copy their `blockedBy` array verbatim: `[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]`; both rows are `blocked` with `fallbackDecision: deferred` (`fallbackNotes`: `DataGrid-backed-flat-audit-list` for ui-11, `flat-audit-summary-policy` for ui-12). Note the Audit Evidence Receipt + compensating-recovery patterns are cross-cutting and are also consumed by the command rows `ui-13`/`ui-14`/`ui-15`. Do NOT invent new dependency IDs, column names, or `ui-NN` keys.
  - [x] Decide output location: create a new `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` (no audit-evidence/recovery pattern spec exists today; `docs/` has the dependency map, operations-shell spec, Phase 2 backlog, truth-state spec, and remove-user journey spec). If you instead extend an existing doc, justify why a new artifact is not warranted and avoid contradicting existing source-of-truth language.
- [x] Specify audit entry points and the missing-capability fallback rule. (AC: 1)
  - [x] Enumerate the five audit entry points verbatim from the shell spec: global navigation (Audit area), tenant rows, tenant detail (audit section), user lookup, and command result. State that audit is **reachable from any access-review context** but never the only path to proof. [Source: `docs/tenants-ui-operations-shell-spec.md#1.2`; `#6. Audit Entry-Point Context`; `_bmad-output/planning-artifacts/ux-design-specification.md#Navigation Patterns`; AC1]
  - [x] Require each entry point to carry the relevant tenant and/or user scope into the audit context (scoped query), not a global unscoped jump. [Source: `docs/tenants-ui-operations-shell-spec.md#1.3 Navigation map`; AC1]
  - [x] State that when audit capability is missing/unproven, the entry point must surface a **documented fallback or blocked dependency** (not a dead link or a fake-success state). Tie missing audit capability to `FC-AUD` (`missing`) and the `missing audit proof` unavailable-action reason. [Source: `docs/tenants-ui-truth-state-and-action-availability-spec.md` §4.4; `docs/tenants-ui-frontcomposer-dependency-map.md#FC-AUD`; AC1]
- [x] Specify the flat audit DataGrid fallback and keep the timeline dependency status visible. (AC: 2)
  - [x] State that, because the reusable `<AuditTimeline>` component is `missing` (`FC-AUD`, no verified FrontComposer source path), the **approved first-slice fallback is a flat audit DataGrid** with: stable ordering, date/type filters, loading, empty, filtered-empty, error, and **accessible expansion** states. Do NOT claim an `<AuditTimeline>` component exists. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Flat Audit List Fallback`; `docs/tenants-ui-frontcomposer-dependency-map.md#FC-AUD`; AC2]
  - [x] Require the reusable timeline dependency status (`FC-AUD: missing`, grouped-mode fast-follow) to **remain visible in the dependency map / backlog** so the fallback is recorded as a deferred decision with an owner and replacement path — not silently treated as the permanent design. Grouped audit mode stays fast-follow unless product/UX explicitly promotes it. [Source: `docs/tenants-ui-frontcomposer-dependency-map.md#Story 12.2 Audit and Consequence Readiness`; `docs/tenants-ui-phase-2-story-backlog.md#Deferred Decisions`; AC2]
  - [x] Source the audit list/fallback only from the existing read endpoint `GET /api/tenants/{tenantId}/audit` (handler `GetTenantAuditQuery`, rows shaped as `TenantAuditEntry`). Cursor-based pagination only — signed, opaque, scope-bound cursors; never offset/limit. Note the 500-event evidence target (or an approved bounded-rendering fallback). No new audit/read endpoint. [Source: `_bmad-output/project-context.md#API Surface`; `src/Hexalith.Tenants.Contracts/Queries/GetTenantAuditQuery.cs`; `docs/tenants-ui-phase-2-story-backlog.md` (`ui-11`)]
- [x] Specify the Audit Evidence Receipt content and copy-safety. (AC: 3)
  - [x] Enumerate the receipt content exactly: actor, target, tenant scope, outcome, timestamp, projection marker, audit reference, and support-safe command reference **where available**. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Audit Evidence Receipt`; AC3]
  - [x] **Critical sourcing insight — do NOT add a backend "receipt" endpoint.** The receipt composes from the existing audit read model: `TenantAuditEntry` already exposes `ActorId` (actor), `Target` (resolved from `NarrativePayload`), `Scope`/`TenantId` (tenant scope), `Outcome`/`EventType` (outcome), `Timestamp`, and `EventId` (audit reference); the projection marker and support-safe command reference come from the read-model freshness marker and the client-tracked `FC-CMD` command lifecycle. [Source: `src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`; `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`]
  - [x] State the copy-safety rule: copyable references must **never expose** raw command/event payloads, bearer tokens, stack traces, internal correlation IDs, internal exception text, or PII. Reference content is limited to support-safe tokens (support-safe command reference, tenant/user reference, projection version/freshness marker, accepted timestamp, audit event reference or fallback state). Note that `NarrativePayload` is a structured, support-safe narrative — not the raw persisted event payload. [Source: `docs/tenants-ui-operations-shell-spec.md#5.3 Support-safe references`; `_bmad-output/project-context.md#Logging & Telemetry`; `_bmad-output/project-context.md#Rejection Event Payloads`; AC3]
  - [x] Cover the **partial-completion** case: when an access change completes or **partially** completes (accepted but projection/audit not yet reconciled), the receipt must show the actual lifecycle state (e.g. `audit pending`) rather than a finished receipt. [Source: `docs/tenants-ui-truth-state-and-action-availability-spec.md` §5.2 non-collapse invariant; AC3]
- [x] Specify delayed/unavailable audit-proof states and the no-false-success rule. (AC: 4)
  - [x] Distinguish the four states explicitly: **audit pending**, **audit delayed**, **audit unavailable**, and **missing implementation support** (audit capability not built / `FC-AUD` not ready). Do not collapse "delayed proof" with "no implementation support". [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Empty, Loading, Stale, and Degraded States` ("Audit unavailable: distinguish delayed evidence from missing implementation support"); `docs/tenants-ui-truth-state-and-action-availability-spec.md` §1/§5.1; AC4]
  - [x] State the no-false-success rule: when proof cannot be verified, the UI **avoids success language** and offers concrete next actions (wait/refresh, retry status lookup, inspect audit later, continue read-only, cite support-safe reference, escalate). Reuse the non-collapse invariant — `accepted`/`confirmed`/`audit available` are never merged into one "success". [Source: `docs/tenants-ui-truth-state-and-action-availability-spec.md` §5.2; `_bmad-output/planning-artifacts/ux-design-specification.md#Feedback Patterns`; AC4]
  - [x] Reaffirm SignalR is a **freshness nudge only** — a nudge may prompt a re-query but must never advance audit evidence to `audit available` or imply proof exists. [Source: `docs/tenants-ui-truth-state-and-action-availability-spec.md` §3.4; AC4]
- [x] Specify compensating-recovery language and flow. (AC: 5)
  - [x] State the language rule: recovery uses explicit **compensating-command terms** — "start correction", "restore intended access", "retry status lookup", "inspect audit", "escalate" — and is **never** labeled as undo, rollback, or hidden edit. The original event remains in the audit trail; the correction is a **new** explicit command with its own consequence preview and proof. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Compensating Recovery`; `#2.5 Experience Mechanics`; `docs/tenants-ui-truth-state-and-action-availability-spec.md` §5.4; AC5]
  - [x] Describe the compensating-recovery flow (UX Journey 4): from audit evidence detail, on discovering a wrong access change → start compensating command → preview correction against **current** state → submit a new command and link both audit records. List useful recovery paths: reassign tenant owner, restore intended access via a new `AddUserToTenant` command, retry access removal, open audit evidence, or escalate with a support-safe reference. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Journey 4: Audit Evidence and Compensating Recovery`; `#2.5 Experience Mechanics`; AC5]
  - [x] **Critical backend truth — corrections are forward commands, not state edits.** "Restore intended access" = a new `AddUserToTenant` (or role) command; there is no undo/rollback API and the UI must not imply one. Note the empty-tenant bootstrap boundary: `AddUserToTenant` skips owner-only RBAC when `state.HasMembershipHistory == false` — informative for the restore-after-last-owner-removal narrative, since last-owner removal is allowed by design. Do NOT invent a "≥1 owner" backend invariant or an undo endpoint. [Source: `_bmad-output/project-context.md#Aggregates`; `_bmad-output/project-context.md#Domain Correctness`; AC5]
  - [x] Keep command domains distinct in recovery copy: tenant-membership corrections target the `tenants` domain (`AddUserToTenant`/`RemoveUserFromTenant`); platform global-administrator corrections are a separate command on the `global-administrators` singleton domain (`SetGlobalAdministrator`/`RemoveGlobalAdministrator`, backlog `ui-15`). Do not conflate them. [Source: `_bmad-output/project-context.md#Identity Scheme`; `docs/tenants-ui-phase-2-story-backlog.md` (`ui-15`)]
- [x] Record per-pattern consumption mapping and future-story readiness. (AC: 1-5)
  - [x] Bind the audit-surface patterns to consuming rows `ui-11-audit-trail-flat-timeline` and `ui-12-tenant-detail-audit-tab`; copy their `blockedBy` array verbatim (`[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]`) and their `backendEvidence`. Note the Audit Evidence Receipt + compensating-recovery patterns are also consumed by the command rows `ui-13`/`ui-14`/`ui-15`. Keep all rows `blocked`; none becomes implementation-ready in this story. [Source: `docs/tenants-ui-phase-2-story-backlog.md#Candidate UI Stories`]
  - [x] State the Implementation Story Rules a future UI story must satisfy to implement these patterns: source projection/query (`GetTenantAuditQuery`), freshness state shown, authorization + unavailable-action reason, audit-evidence path or approved flat-DataGrid fallback, Audit Evidence Receipt content + copy-safety, distinct delayed/unavailable/missing-support states, compensating-recovery language, support-safe references, accessibility behavior, and localization responsibility. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Implementation Story Rules`]
  - [x] Confirm grouped audit mode, server-side anomaly scoring, and advanced analytics stay out of the first slice (fast-follow) unless product explicitly promotes them. [Source: `docs/tenants-ui-phase-2-story-backlog.md#Scope Boundary`]
- [x] Record support-safe references and accessibility/localization contracts shared with other Epic 9 stories. (AC: 1-5)
  - [x] Support-safe references for audit/command troubleshooting must never expose raw payloads, bearer tokens, stack traces, internal correlation IDs, or PII. [Source: `docs/tenants-ui-operations-shell-spec.md#5.3`; `_bmad-output/project-context.md#Logging & Telemetry`]
  - [x] Accessibility: audit availability/delay states, receipt content, flat-list row expansion, and compensating-recovery prompts must be perceivable without color and announced via live status; keyboard users must be able to scan the audit list, expand rows, copy a support-safe reference, and start/exit a compensating-command flow. Tie to `FC-A11Y`. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Accessibility`; AC1-5]
  - [x] Require stable selectors / component contracts (not arbitrary row text) for automation of audit-state and receipt assertions, tied to `FC-A11Y`/`FC-DOC`. Localize all state labels, timestamps, outcomes, audit-state words, and recovery actions. Tie to `FC-L10N`. [Source: `docs/tenants-ui-operations-shell-spec.md#5.2`; `docs/tenants-ui-truth-state-and-action-availability-spec.md` §9.3; AC2-5]
- [x] Record implementation evidence and run documentation validation. (AC: 1-5)
  - [x] Run `git diff --check` after documentation edits.
  - [x] Confirm no source code, package versions, lockfiles, generated files, submodule pointers (`git diff --submodule=short` empty for all root submodules), backend story statuses, UI screens, endpoints, commands, queries, or Phase 1 release gates changed.
  - [x] Update this story's Dev Agent Record with files changed, validation performed, and unresolved decisions.

## Dev Notes

### Scope Guard (read first)

- Epic 9 is readiness/planning-only. This story produces an **audit-evidence and compensating-recovery UI pattern specification** — the deferred patterns referenced (but not defined) by the Story 9.4 remove-user journey: the Audit Evidence Receipt, the flat audit DataGrid fallback, the delayed/unavailable audit-proof state distinctions, the audit entry-point contract, and the compensating-command recovery language/flow. It is **not** shippable Admin UI and must not route a Developer agent into product UI delivery until separate Phase 2 implementation stories are created. [Source: `_bmad-output/planning-artifacts/epics.md#Epic 9`]
- Output is documentation only. It does NOT create UI components, Blazor pages/routes, backend endpoints, commands, queries, package references, generated files, or Phase 1 release gates. [Source: `_bmad-output/planning-artifacts/epics.md#Story 9.5`]
- Backend MVP stories remain unblocked by UI dependency readiness. [Source: `_bmad-output/project-context.md#BMAD Workflow`]

### This story composes existing patterns — it does NOT redefine them

- Story 9.3's `docs/tenants-ui-truth-state-and-action-availability-spec.md` is the canonical owner of the truth-state badge vocabulary, the layered feedback states (including the audit-evidence states `audit pending` / `audit available` / `delayed` / `unavailable` / `approved fallback`), the `missing audit proof → FC-AUD` reason mapping (§4.4), the non-collapse invariant (§5.2), the concurrency/recovery table (§5.4), and the "never label recovery as undo" rule (§5.4). This story **applies** those to the audit/recovery patterns; it must stay consistent and must not re-enumerate or contradict them. [Source: `docs/tenants-ui-truth-state-and-action-availability-spec.md`]
- Story 9.2's `docs/tenants-ui-operations-shell-spec.md` owns the audit **entry points** (§1.2, §6) and support-safe references (§5.3). The audit surface implementation itself is `blocked`/`planning-only` and owned by later stories; this spec defines the **patterns**, not the screens.
- Story 9.1's `docs/tenants-ui-frontcomposer-dependency-map.md` owns the 10 fixed dependency IDs and the `FC-AUD: missing` audit evidence. Reuse them verbatim; add none.
- Story 9.4's `docs/tenants-ui-remove-user-from-tenant-journey-spec.md` explicitly defers the full Audit Evidence Receipt, flat audit DataGrid fallback, and compensating-recovery UI patterns to **this** story. Keep the receipt/recovery patterns consistent with the 9.4 journey's `audit_pending`/`audit_available` states and its support-safe command reference.

### Previous Story Intelligence (Stories 9.1–9.4 — done)

- **Reconcile, do not duplicate.** Story 9.1's review penalized an unnecessary parallel artifact and reverted a wrong endpoint normalization; Stories 9.2/9.3/9.4 reused the dependency map, shell spec, truth-state spec, and backlog as copy-forward source of truth and copied `blockedBy`/`backendEvidence` arrays verbatim. This audit/recovery spec adds the deferred *audit-evidence and compensating-recovery pattern* layer on top of those; reference dependency IDs, backlog rows, and the 9.3 truth-state patterns rather than redefining them. [Source: `_bmad-output/implementation-artifacts/9-1-map-fluent-ui-and-frontcomposer-dependencies-for-tenant-admin-screens.md#Senior Developer Review (AI)`; `_bmad-output/implementation-artifacts/9-4-specify-the-removeuserfromtenant-command-capable-journey.md`]
- **Command endpoint route is `POST /api/v1/commands`** (FrontComposer `EventStoreOptions.CommandEndpointPath` default); `/api/commands` is recorded as an *alias* to confirm against the deployed gateway. Compensating commands ("restore intended access", "start correction") dispatch to the same command surface. [Source: `docs/tenants-ui-frontcomposer-dependency-map.md#Command Endpoint Route Evidence`; `_bmad-output/project-context.md#API Surface`]
- **Dependency ID catalog is fixed** at 10 IDs (`FC-TBL`, `FC-LYT`, `FC-CMD`, `FC-CNC`, `FC-AUD`, `FC-CNS`, `FC-TOK`, `FC-A11Y`, `FC-L10N`, `FC-DOC`). `FC-AUD` (audit timeline) is `missing`; `FC-TOK` is `missing` for timeline/consequence tokens. These keep `ui-11`/`ui-12` `blocked`. Do not add new IDs. [Source: `docs/tenants-ui-frontcomposer-dependency-map.md#Dependency ID Catalog`]

### CRITICAL sourcing insight — the Audit Evidence Receipt composes from the existing read model (no new endpoint)

- The `TenantAuditEntry` read DTO (`src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`) **already exposes the receipt fields**: `ActorId` (actor), `Target` (resolved from `NarrativePayload` userId/key, falling back to tenantId), `Scope`/`TenantId` (tenant scope), `Outcome`/`EventType` (outcome), `Timestamp`, `EventId` (audit reference), and `Category` + `NarrativePayload` (structured, support-safe narrative). The projection marker and support-safe command reference come from the read-model freshness marker and the client-tracked `FC-CMD` command lifecycle — **not** a backend "receipt" or "consequence" endpoint. Specifying a new backend endpoint would contradict the architecture (consequence/receipt compose from read models; command lifecycle is client-side). [Source: `src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`; `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`]
- `NarrativePayload` is a structured, support-safe narrative — **not** the raw persisted event payload. The copy-safety rule (AC3) still holds: never expose raw payloads, tokens, stack traces, internal correlation IDs, or internal exception text in copyable references.

### CRITICAL backend truth — corrections are forward commands; there is no undo (do NOT invent one)

- Recovery is always a **new compensating command** through the normal command pipeline — never an undo/rollback API. "Restore intended access" = a new `AddUserToTenant` (or role) command; "start correction" = a new explicit command with its own consequence preview and proof. The original event remains in the immutable audit trail. A future implementer must NOT add an undo endpoint or model recovery as a hidden edit. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Compensating Recovery`; `#2.5 Experience Mechanics`; `_bmad-output/project-context.md#Domain Correctness`]
- `TenantAggregate` does **NOT** enforce a "must retain ≥1 owner" invariant — last-owner removal is **allowed by design**. The compensating-recovery narrative (restore an owner after an erroneous last-owner removal) therefore relies on a forward `AddUserToTenant`; the empty-tenant bootstrap path (`AddUserToTenant` skips owner-only RBAC when `state.HasMembershipHistory == false`) is the relevant boundary. Do NOT invent a "≥1 owner" backend invariant. [Source: `_bmad-output/project-context.md#Aggregates`]

### Domains are distinct (prevent conflation)

- Tenant-membership corrections target the `tenants` domain (`AddUserToTenant` / `RemoveUserFromTenant`, `AggregateId` = managed tenant ID). Platform global-administrator corrections are a **separate** command on the singleton `global-administrators` domain/aggregate (`SetGlobalAdministrator` / `RemoveGlobalAdministrator`, backlog row `ui-15`). Recovery copy must keep these distinct; a global-admin authority correction does not edit a tenant aggregate. [Source: `_bmad-output/project-context.md#Identity Scheme`; `docs/tenants-ui-phase-2-story-backlog.md` (`ui-15`)]

### Numbering hazard — two different `9-x`/`10-x`/`11-x`/`12-x` namespaces (read carefully)

- This story's key is the **sprint-status Epic 9 UI-planning** key `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns`. The `backendEvidence`/`evidenceSource` arrays inside `docs/tenants-ui-phase-2-story-backlog.md` and the dependency map (e.g. `ui-11` cites `post-epic-5-r5a3-tenant-audit-projection-query`, `10-2-audit-projection-write-safety`; `FC-AUD` sourced from `12-2-audit-timeline-and-consequence-preview-readiness`) reference a **separate Phase 2 backend/FrontComposer backlog** that also uses `9-x`/`10-x`/`11-x`/`12-x` keys. These are NOT this epic's stories. Do not conflate the two namespaces or mark backend rows complete based on this UI story. [Source: `docs/tenants-ui-phase-2-story-backlog.md`; `_bmad-output/implementation-artifacts/sprint-status.yaml`]

### Pattern shape (AC1–AC5) — canonical source

- **Audit entry points (AC1):** global navigation, tenant rows, tenant detail, user lookup, command result → audit context; audit is always reachable but never the only proof path; missing capability = documented fallback or blocked dependency (`FC-AUD` / `missing audit proof`). [Source: `docs/tenants-ui-operations-shell-spec.md#1.2`, `#6`; `_bmad-output/planning-artifacts/ux-design-specification.md#Navigation Patterns`]
- **Flat audit DataGrid fallback (AC2):** `<AuditTimeline>` is `missing`; approved first-slice fallback is a flat DataGrid with stable ordering, filters, loading, empty, error, accessible expansion, sourced from `GET /api/tenants/{tenantId}/audit` (cursor-based, 500-event target); reusable timeline status stays visible as a deferred decision; grouped mode is fast-follow. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Flat Audit List Fallback`; `docs/tenants-ui-frontcomposer-dependency-map.md#FC-AUD`]
- **Audit Evidence Receipt (AC3):** actor, target, tenant scope, outcome, timestamp, projection marker, audit reference, support-safe command reference (where available); composes from `TenantAuditEntry` + read-model freshness + client command lifecycle; copy-safe (no raw payloads/tokens/traces/internals). [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Audit Evidence Receipt`; `src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`]
- **Delayed/unavailable proof (AC4):** distinguish audit pending / audit delayed / audit unavailable / missing implementation support; avoid success language when proof can't be verified; SignalR is a nudge only. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Empty, Loading, Stale, and Degraded States`; `docs/tenants-ui-truth-state-and-action-availability-spec.md` §5.2, §3.4]
- **Compensating recovery (AC5):** explicit compensating-command terms (start correction, restore intended access, retry status lookup, inspect audit, escalate); never undo/rollback/hidden edit; correction is a new forward command with its own preview and proof, linked to the original audit record. [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Compensating Recovery`; `#Journey 4`; `docs/tenants-ui-truth-state-and-action-availability-spec.md` §5.4]
- Custom components this spec *governs but does NOT implement*: Audit Evidence Receipt (`FC-AUD`/`FC-CMD`/`FC-TOK`), Flat Audit List Fallback (`FC-AUD` missing → DataGrid via `FC-TBL`), plus the Truth State Badge / Command Lifecycle Panel audit states (referenced from 9.3, not redefined). [Source: `_bmad-output/planning-artifacts/ux-design-specification.md#Custom Components`; `docs/tenants-ui-frontcomposer-dependency-map.md#Dependency ID Catalog`]

### Pattern-to-backlog consumption mapping (copy `blockedBy`/`backendEvidence` verbatim; do not re-derive)

- **Audit-surface consuming rows:**
  - `ui-11-audit-trail-flat-timeline` — status `blocked`; `fallbackDecision` `deferred`; `fallbackNotes` `DataGrid-backed-flat-audit-list`; owner `Tenants Product/UX + Hexalith.FrontComposer`.
    - `blockedBy` (verbatim): `[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]`.
    - `backendEvidence` (verbatim): `[post-epic-5-r5a3-tenant-audit-projection-query, 9-1-opaque-signed-query-cursors, 9-2-stable-cursor-pagination-under-role-and-membership-changes, 9-4-actor-layer-query-guardrails, 10-2-audit-projection-write-safety, 10-4-projection-write-conformance-and-recovery-tests, endpoint:GET /api/tenants/{tenantId}/audit]`.
  - `ui-12-tenant-detail-audit-tab` — status `blocked`; `fallbackDecision` `deferred`; `fallbackNotes` `flat-audit-summary-policy`; owner `Tenants Product/UX + Hexalith.FrontComposer`.
    - `blockedBy` (verbatim): `[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]`.
    - `backendEvidence` (verbatim): `[post-epic-5-r5a3-tenant-audit-projection-query, 10-2-audit-projection-write-safety, 10-4-projection-write-conformance-and-recovery-tests, endpoint:GET /api/tenants/{tenantId}/audit]`.
- **Cross-cutting (receipt + compensating recovery):** consumed by command rows `ui-13-disable-or-enable-tenant`, `ui-14-user-management-remove-user`, and `ui-15-global-admin-command-management` (each `blocked`, `blockedBy: [FC-LYT, FC-CMD, FC-CNC, FC-CNS, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]`). Reference these; do not re-bind or change their arrays (`ui-14` is owned by Story 9.4).
- All consuming rows stay `blocked` (`FC-AUD`/`FC-CNS` `missing` + deferred high-impact fallbacks). None becomes implementation-ready here. [Source: `docs/tenants-ui-phase-2-story-backlog.md#Candidate UI Stories`; `#Blocked`]

### Backend and Data Boundaries

- Audit evidence comes only from the existing read endpoint `GET /api/tenants/{tenantId}/audit` (`GetTenantAuditQuery`, rows = `TenantAuditEntry`). Cursor-based pagination only — signed, opaque, scope-bound cursors; never offset/limit. ETag `If-None-Match` → `304` is the freshness primitive served by `CachingProjectionActor`. No new audit/receipt/consequence endpoint. [Source: `_bmad-output/project-context.md#API Surface`; `#Projections`]
- Compensating commands dispatch to `POST /api/v1/commands` (e.g. `command:AddUserToTenant` for "restore intended access"). Command lifecycle is tracked client-side via FrontComposer command/feedback services (`FC-CMD`); SignalR notifications are refresh nudges only, never proof. [Source: `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`; `_bmad-output/project-context.md#API Surface`]
- Rejection events carry structured data only; user-facing text is composed at the HTTP boundary by `RejectionToHttpStatusMapper` (RFC 7807: 404 not-found, 409 conflict, 422 other). Audit/recovery copy must map rejections to safe localized text — never raw payloads, stack traces, tokens, internal correlation IDs, or internal exception text. [Source: `_bmad-output/project-context.md#API Surface`; `#Rejection Event Payloads`]
- Identity: use JWT `sub` (`envelope.UserId`) for actor identity, never `name`/`email`. IDs are ULIDs, not GUIDs. Authorization is enforced server-side (L1 API gate + L2 domain RBAC); the UI reflects authorization state and unavailable-action reasons but is not the authorization boundary. [Source: `_bmad-output/project-context.md#Identity Scheme`; `#Authorization (RBAC)`]

### Project Structure Notes

- Documentation outputs belong under `docs/` and this story's Dev Agent Record. No audit-evidence/compensating-recovery pattern spec exists yet (`docs/` has `tenants-ui-frontcomposer-dependency-map.md`, `tenants-ui-operations-shell-spec.md`, `tenants-ui-phase-2-story-backlog.md`, `tenants-ui-truth-state-and-action-availability-spec.md`, and `tenants-ui-remove-user-from-tenant-journey-spec.md`), so a new `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` is the natural home. Future Phase 2 UI assets belong in a dedicated FrontComposer adapter/UI project only after readiness conversion, not in backend packages. [Source: `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`]
- Preserve root-level submodule policy. Reading `Hexalith.FrontComposer` is allowed; never initialize nested submodules or run recursive submodule commands. [Source: `CLAUDE.md#Submodule Policy`]
- `_bmad-output/` is untracked — do not commit it in code PRs. Use central package management; add no package versions in this documentation story. [Source: `_bmad-output/project-context.md#BMAD Workflow`]

### Testing / Validation Requirements

- No source-code test suite is required: this story updates planning/specification documentation only. [Source: `_bmad-output/project-context.md#BMAD Workflow`]
- Required documentation validation:
  - `git diff --check`.
  - Manual AC checklist against this story (AC1 five audit entry points + missing-capability fallback/blocked; AC2 flat audit DataGrid fallback states + visible timeline dependency status; AC3 receipt content set + copy-safety + partial-completion; AC4 four distinct delayed/unavailable/missing-support states + no-false-success; AC5 explicit compensating-command terms + never-undo + forward-command recovery flow).
  - Consistency check against `docs/tenants-ui-truth-state-and-action-availability-spec.md` (audit-evidence feedback states, non-collapse invariant §5.2, `missing audit proof → FC-AUD` §4.4, never-label-undo §5.4 — referenced, not redefined), `docs/tenants-ui-frontcomposer-dependency-map.md` (10 fixed IDs; `FC-AUD: missing`; no new IDs), and `docs/tenants-ui-remove-user-from-tenant-journey-spec.md` (receipt/recovery patterns deferred to 9.5 are now supplied consistently).
  - Per-pattern consumption check: the spec names consuming rows `ui-11`/`ui-12` (and cross-cutting `ui-13`/`ui-14`/`ui-15`) and copies their literal `blockedBy`/`backendEvidence` arrays; no new dependency IDs, column names, or `ui-NN` keys introduced; rows stay `blocked`.
  - Backend-truth checks: no new backend audit/receipt/consequence/undo endpoint proposed; the receipt composes from `TenantAuditEntry`; recovery is a forward compensating command; no invented "≥1 owner" invariant; no `tenants` / `global-administrators` domain conflation.
  - Change-boundary check confirming no source code, package files, generated artifacts, submodule pointers, backend endpoints/commands/queries, UI screens, or Phase 1 gates changed.

### References

- `_bmad-output/planning-artifacts/epics.md#Epic 9: Administrators Can Plan Phase 2 UI Access Operations Safely`
- `_bmad-output/planning-artifacts/epics.md#Story 9.5: Specify Audit Evidence and Compensating Recovery UI Patterns`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Journey 4: Audit Evidence and Compensating Recovery`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Audit Evidence Receipt`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Flat Audit List Fallback`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Compensating Recovery`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Empty, Loading, Stale, and Degraded States`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Feedback Patterns`
- `_bmad-output/planning-artifacts/ux-design-specification.md#2.5 Experience Mechanics`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Implementation Story Rules`
- `_bmad-output/planning-artifacts/ux-design-specification.md#Navigation Patterns`
- `_bmad-output/planning-artifacts/architecture.md#Frontend Architecture`
- `docs/tenants-ui-truth-state-and-action-availability-spec.md` (§1, §4.4, §5.1, §5.2, §5.4, §3.4, §9.3)
- `docs/tenants-ui-operations-shell-spec.md` (§1.2, §5.2, §5.3, §6)
- `docs/tenants-ui-frontcomposer-dependency-map.md#Dependency ID Catalog` (`FC-AUD`); `#Story 12.2 Audit and Consequence Readiness`
- `docs/tenants-ui-phase-2-story-backlog.md#Candidate UI Stories` (`ui-11`, `ui-12`, `ui-13`, `ui-14`, `ui-15`); `#Deferred Decisions`
- `docs/tenants-ui-remove-user-from-tenant-journey-spec.md` (audit/recovery patterns deferred to Story 9.5)
- `_bmad-output/implementation-artifacts/9-4-specify-the-removeuserfromtenant-command-capable-journey.md`
- `src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`; `src/Hexalith.Tenants.Contracts/Queries/GetTenantAuditQuery.cs`; `src/Hexalith.Tenants.Contracts/Enums/AuditEventCategory.cs`
- `_bmad-output/project-context.md#Aggregates`
- `_bmad-output/project-context.md#Identity Scheme`
- `_bmad-output/project-context.md#API Surface`
- `_bmad-output/project-context.md#Logging & Telemetry`
- `_bmad-output/project-context.md#Rejection Event Payloads`

## Dev Agent Record

### Agent Model Used

Claude Opus 4.8 (1M context) — create-story context engine; dev-story implementation (non-interactive run)

### Debug Log References

- 2026-06-02 - Resolved BMAD create-story customization (`resolve_customization.py` shape: empty prepend/append, `persistent_facts: **/project-context.md`, no `on_complete`); loaded config, sprint status, project context, planning artifacts (epics Epic 9 + Story 9.5, ux-design-specification Journey 4 / Audit Evidence Receipt / Flat Audit List Fallback / Compensating Recovery / Empty-Loading-Stale-Degraded / Feedback Patterns / Implementation Story Rules, architecture Frontend), existing UI docs (dependency map FC-AUD, operations-shell spec audit entry points §1.2/§6, Phase 2 backlog audit rows, 9.3 truth-state spec audit/feedback states, 9.4 remove-user journey deferrals), and backend audit read-model types.
- 2026-06-02 - Verified Story 9.5 target key from `sprint-status.yaml`: `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` (status `backlog`); epic-9 already `in-progress` (9.1–9.4 done), no epic status change required (not the first story in the epic).
- 2026-06-02 - Confirmed no existing `docs/` audit-evidence/recovery pattern spec; Story 9.5 produces a new `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` that supplies the patterns Story 9.4 deferred (Audit Evidence Receipt, flat audit DataGrid fallback, delayed/unavailable proof distinctions, audit entry-point contract, compensating-command recovery), composing the 9.3 truth-state/feedback contract, the 9.2 shell entry points, and the 9.1 dependency map.
- 2026-06-02 - Captured AC1–AC5 source mapping and bound the audit-surface patterns to consuming backlog rows `ui-11-audit-trail-flat-timeline` and `ui-12-tenant-detail-audit-tab` with verbatim `blockedBy` `[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]` and `backendEvidence`; flagged the cross-cutting receipt/recovery consumption by command rows `ui-13`/`ui-14`/`ui-15`. Flagged disaster-prevention insights: (1) the Audit Evidence Receipt composes from the existing `TenantAuditEntry` read model (`ActorId`/`Target`/`Scope`/`Outcome`/`Timestamp`/`EventId`/`NarrativePayload`) + read-model freshness + client `FC-CMD` lifecycle — NO new backend receipt/consequence endpoint; `NarrativePayload` is support-safe, not the raw event payload; (2) corrections are forward compensating commands (`AddUserToTenant` for "restore intended access") — there is NO undo/rollback API, and last-owner removal is allowed by design (no "≥1 owner" invariant to invent); (3) `tenants` vs `global-administrators` domains must not be conflated in recovery copy; (4) the dual `9-x`/`10-x`/`11-x`/`12-x` namespace hazard between sprint-status Epic 9 and the backlog `backendEvidence`/`FC-AUD` source keys.
- 2026-06-02 (dev-story) - Re-read the four source-of-truth UI docs (9.1 dependency map, 9.2 operations-shell spec, 9.3 truth-state spec, 9.4 remove-user journey), the Phase 2 backlog, the UX spec audit/recovery sections (Journey 4, Audit Evidence Receipt, Flat Audit List Fallback, Compensating Recovery, Empty/Loading/Stale/Degraded, Feedback Patterns, Navigation Patterns, Implementation Story Rules), and the backend contracts (`TenantAuditEntry`, `GetTenantAuditQuery`, `AuditEventCategory`). Authored `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` (§1 entry points, §2 flat-DataGrid fallback, §3 receipt + copy-safety, §4 four delayed/unavailable/missing-support states, §5 compensating-recovery, §6 consumption mapping + Implementation Story Rules, §7 a11y/l10n, §8 backend boundaries, §9 AC traceability).
- 2026-06-02 (dev-story) - Validation: `git diff --check` clean; `git diff --submodule=short` empty for all five root submodules; no `src/`, `*.csproj`, `Directory.Packages.props`, `*.sln`, lockfile, or generated-file changes; no backend story-status edits beyond this story's own sprint-status entry. Verified every cited `project-context.md` term exists (`Domain Correctness`, `HasMembershipHistory`, `Aggregates`, `Identity Scheme`, `Rejection Event Payloads`, `RejectionToHttpStatusMapper`, `CachingProjectionActor`); `EventStoreOptions.CommandEndpointPath` is sourced from the dependency map. Verbatim `blockedBy`/`backendEvidence` arrays for `ui-11`/`ui-12` cross-checked against the backlog; no new dependency IDs, column names, or `ui-NN` keys introduced.

### Completion Notes List

- Create-story context engine analysis completed for Story 9.5.
- Existing dependency map, operations-shell spec, Phase 2 UI backlog, 9.3 truth-state spec, and 9.4 remove-user journey treated as copy-forward source of truth to prevent duplicate planning artifacts (carrying forward Story 9.1–9.4 review lessons).
- Story is explicitly scoped to documentation only (planning-only). It supplies the audit-evidence and compensating-recovery patterns that Story 9.4 deferred, by composing — not redefining — the 9.3 truth-state/feedback patterns, the 9.2 audit entry points, and the 9.1 dependency map.
- Three critical disaster-prevention guardrails embedded: (1) the Audit Evidence Receipt composes from the existing `TenantAuditEntry` read model (no new backend receipt/consequence endpoint; `NarrativePayload` is support-safe); (2) recovery is a forward compensating command (`AddUserToTenant`), never an undo/rollback API, and no invented "≥1 owner" invariant; (3) strict separation of `tenants` and `global-administrators` domains in recovery copy.
- **(dev-story) Delivered** `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` — the audit-evidence and compensating-recovery UI pattern specification. All five ACs are covered with a traceability table (§9): AC1 five scoped audit entry points + missing-capability fallback/blocked dependency (§1); AC2 approved flat audit DataGrid fallback states + visible `FC-AUD: missing` timeline status (§2); AC3 eight-field receipt content + copy-safety + partial-completion (§3); AC4 four distinct delayed/unavailable/missing-support states + no-false-success + SignalR-nudge-only (§4); AC5 explicit compensating-command terms + never-undo + forward-command flow + distinct domains (§5).
- Consistency confirmed against the 9.3 truth-state spec (audit-evidence feedback states, non-collapse invariant §5.2, `missing audit proof → FC-AUD` §4.4, never-label-undo §5.4 — referenced, not redefined), the 9.1 dependency map (10 fixed IDs; `FC-AUD: missing`; no new IDs), and the 9.4 remove-user journey (receipt/recovery patterns it deferred are now supplied consistently with `audit_pending`/`audit_available` and the support-safe command reference).
- No source-code test suite required (planning/documentation-only story). Documentation validation (`git diff --check`, submodule-pointer check, change-boundary check, verbatim-array check, backend-truth checks) performed and passed.
- Unresolved decisions (intentionally left to future stories): `ui-11`/`ui-12`/`ui-13`/`ui-14`/`ui-15` remain `blocked`; the flat-DataGrid fallback and reduced-feedback/consequence-preview fallbacks are recorded as `deferred` decisions owned by Tenants Product/UX + `Hexalith.FrontComposer`; grouped audit mode stays fast-follow. No row becomes implementation-ready in this story.

### File List

- `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` (new) — the audit-evidence and compensating-recovery UI pattern specification (the Story 9.5 deliverable). Senior review removed stray tool-call XML from the file end. (tracked)
- `_bmad-output/implementation-artifacts/9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns.md` (modified) — task checkboxes marked complete, Dev Agent Record/Change Log updated, Status → review. (untracked planning artifact)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified) — `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` status → review; `last_updated` 2026-06-02. (untracked planning artifact)

## Senior Developer Review (AI)

**Reviewer:** Jerome (AI adversarial review, non-interactive story-automator flow)
**Date:** 2026-06-02
**Outcome:** Approve (1 defect found and auto-fixed; 0 critical issues remain)

### Scope of review

Adversarial review of the Story 9.5 deliverable `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` against the five Acceptance Criteria, the story Tasks/Subtasks (all marked `[x]`), and the cited sources of truth. Per the Epic 9 planning-only boundary, this is a documentation/specification story — no source-code test suite applies. `_bmad-output/` and tooling/config folders were excluded from the source review.

### Findings

| # | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | HIGH | The published spec ended with leaked tool-call markup — stray `</content>` and `</invoke>` XML tags on the final two lines (313–314), an artifact of the authoring Write call. This corrupts the deliverable's integrity and would render as raw text. | **Fixed** — removed the two stray lines; file now ends cleanly at the References list. Re-scan confirms no remaining leaked tags (`</invoke>`/`</content>`/`<antml`), balanced code fences, and `git diff --check` clean. |

No CRITICAL issues found. No false File List claims, no fabricated dependency IDs, no invented backend endpoints/invariants.

### Verification performed (claims independently confirmed against source)

- **AC1–AC5 all implemented** and mapped in the spec's §9 traceability table; each AC's substance is present in §1–§5.
- **Backend-truth claims verified against actual code:** `TenantAuditEntry` (`src/Hexalith.Tenants.Contracts/Queries/TenantAuditEntry.cs`) exposes exactly the receipt fields the §3.2 source table claims — `EventId`, `EventType`, `Category`, `ActorId`, `Timestamp`, `TenantId`, `NarrativePayload`, plus computed `Target` (resolves `NarrativePayload["userId"]` → `["key"]` → `TenantId`), `Scope` (= `TenantId`), and `Outcome` (= `EventType`). `AuditEventCategory` enum = `Access` / `Administrative` as cited. No new backend receipt/consequence/undo endpoint is proposed; recovery is a forward `AddUserToTenant` command; last-owner removal is correctly described as allowed by design with no invented "≥1 owner" invariant.
- **Verbatim dependency arrays cross-checked** against `docs/tenants-ui-phase-2-story-backlog.md`: `ui-11`/`ui-12` `blockedBy` `[FC-LYT, FC-AUD, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]` and their `backendEvidence` arrays match exactly; cross-cutting `ui-13`/`ui-14`/`ui-15` `blockedBy` `[FC-LYT, FC-CMD, FC-CNC, FC-CNS, FC-TOK, FC-A11Y, FC-L10N, FC-DOC]` matches. All consuming rows remain `blocked`; no new `FC-*` IDs, column names, or `ui-NN` keys introduced.
- **Cross-references resolve:** every cited section in `docs/tenants-ui-truth-state-and-action-availability-spec.md` (§3.4, §4.1/§4.2/§4.4, §5.1/§5.2/§5.4, §6.3, §9.1–§9.4, §10) and every cited UX-spec anchor (Journey 4, Audit Evidence Receipt, Flat Audit List Fallback, Compensating Recovery, Empty/Loading/Stale/Degraded, Implementation Story Rules) exists. Story 9.4's §6.2 "Deferred to Story 9.5" deferral is real and consistent with what this spec supplies.
- **Change-boundary checks:** `git diff --check` clean; `git diff --submodule=short` empty for all five root submodules; no `src/`, `*.csproj`, `Directory.Packages.props`, `*.sln`, lockfile, or generated-file changes; no backend story-status edits beyond this story's own sprint-status entry.

### Decision

All ACs implemented and source-verified; the one HIGH defect was auto-fixed in this review. No critical issues remain → **Status: done**; sprint-status `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` synced to `done`.

## Change Log

| Date | Change |
| --- | --- |
| 2026-06-02 | Created Story 9.5 (planning-only) via create-story context engine. Status → ready-for-dev. Sprint-status `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` → ready-for-dev. |
| 2026-06-02 | dev-story: authored `docs/tenants-ui-audit-evidence-and-compensating-recovery-spec.md` (AC1–AC5: audit entry points + missing-capability fallback, flat audit DataGrid fallback, Audit Evidence Receipt content + copy-safety, delayed/unavailable/missing-support proof states, compensating-recovery language/flow; consumption mapping for `ui-11`/`ui-12` + cross-cutting `ui-13`/`ui-14`/`ui-15`). All tasks/subtasks marked complete. Documentation validation passed (no source/package/submodule changes). Status → review. Sprint-status `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` → review. |
| 2026-06-02 | Senior Developer Review (AI, non-interactive): adversarial review of the spec against AC1–AC5 and all cited sources. Found and auto-fixed 1 HIGH defect — leaked tool-call XML (`</content>`/`</invoke>`) on the deliverable's final two lines. Independently verified `TenantAuditEntry` receipt fields, `AuditEventCategory` values, verbatim `ui-11`/`ui-12`/`ui-13`/`ui-14`/`ui-15` `blockedBy`/`backendEvidence` arrays, all 9.3 truth-state and UX-spec cross-references, and the 9.4 §6.2 deferral. Change-boundary checks clean (no source/package/submodule changes). 0 critical issues remain. Status → done. Sprint-status `9-5-specify-audit-evidence-and-compensating-recovery-ui-patterns` → done. |
