---
title: 'Fix GitHub Actions CI and release failures from runs 28941326396 and 28941326377'
type: 'bugfix'
created: '2026-07-08'
status: 'done'
baseline_commit: 'c016842b36f333cf70edd7d7c15711731c72f8b9'
context: []
---

<frozen-after-approval reason="human-owned intent - do not modify unless human renegotiates">

## Intent

**Problem:** The `CI` run `28941326396` fails after a successful Release build because `scripts/validate-nuget-packages.py` rejects `Hexalith.Tenants.Aspire.0.0.0-ci-test.nupkg` for an unexpected `ModelContextProtocol` dependency. The `Release` run `28941326377` fails in `Hexalith.Tenants.Sample.Tests` because sample event-pipeline tests build envelopes with non-Hexalith message IDs such as `msg-created`, and the shared `EventStoreDomainEventProcessor` now rejects those envelopes before projection handlers run.

**Approach:** Update Tenants-owned validation and tests to match the current published dependency graph and EventStore envelope contract. Preserve the existing root `references/Hexalith.FrontComposer` pointer change as user-owned work, and do not modify submodule contents.

## Boundaries & Constraints

**Always:** Keep package dependency validation strict and explicit. Keep sample tests exercising the real `EventStoreDomainEventProcessor` path. Use `.slnx` only for restore/build and run test projects individually. Follow centralized package versioning, warnings-as-errors, and no recursive submodule commands.

**Ask First:** Halt before changing any submodule contents, reverting the existing `references/Hexalith.FrontComposer` pointer bump, weakening forbidden dependency checks, or changing production EventStore processor behavior in `references/`.

**Never:** Do not remove `Hexalith.Tenants.Aspire` from package validation, bypass validation scripts, make the sample tests assert less behavior, add inline package versions, or replace EventStore message-id validation with GUID parsing.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Aspire package validation | `Hexalith.Tenants.Aspire` package contains the current Aspire/EventStore transitive dependency set including `ModelContextProtocol` | `scripts/validate-nuget-packages.py ./nupkgs` accepts the package only when the full expected set matches | Unexpected or missing dependencies still fail with the existing boundary mismatch error |
| Sample event processing | Test-created `EventStoreDomainEventEnvelope` instances are processed by `EventStoreDomainEventProcessor` | Envelopes use valid Hexalith sortable unique message IDs, projection handlers run, and endpoint reads reflect projected state | Invalid payload tests elsewhere remain untouched and should still fail closed |
| Duplicate/idempotency sensitivity | Multiple events are processed in one sample test | Each envelope gets a distinct valid message ID so the in-memory marker store does not classify a later event as a duplicate | Repeated domain events remain tested as separate deliveries with separate message IDs |

</frozen-after-approval>

## Code Map

- `scripts/validate-nuget-packages.py` -- owns the package ID and dependency allowlist used by CI and release package validation.
- `tests/Hexalith.Tenants.Contracts.Tests/CiQualityGateScriptTests.cs` -- mirrors package dependency fixtures used to test the validator script.
- `samples/Hexalith.Tenants.Sample.Tests/Endpoints/AccessCheckEndpointsTests.cs` -- creates sample access-check event envelopes that currently use invalid message IDs.
- `samples/Hexalith.Tenants.Sample.Tests/Endpoints/TenantConfigurationEndpointsTests.cs` -- creates sample configuration event envelopes that currently use invalid message IDs.
- `references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Subscriptions/EventStoreDomainEventProcessor.cs` -- reference-only contract source showing `MessageId` validation via `UniqueIdHelper.ToGuid`; do not edit.

## Tasks & Acceptance

**Execution:**
- [x] `scripts/validate-nuget-packages.py` -- add `ModelContextProtocol` to the expected `Hexalith.Tenants.Aspire` dependency boundary -- keeps validation aligned with the current package graph while preserving strict mismatch detection.
- [x] `tests/Hexalith.Tenants.Contracts.Tests/CiQualityGateScriptTests.cs` -- add the same `ModelContextProtocol` entry to the synthetic expected-dependency fixture -- keeps script governance tests aligned with the validator.
- [x] `samples/Hexalith.Tenants.Sample.Tests/Endpoints/AccessCheckEndpointsTests.cs` -- make the test envelope helper assign valid, unique Hexalith message IDs while leaving aggregate, tenant, event type, and payload semantics unchanged -- restores processor dispatch through the real EventStore client path.
- [x] `samples/Hexalith.Tenants.Sample.Tests/Endpoints/TenantConfigurationEndpointsTests.cs` -- apply the same valid message-id helper behavior for configuration event pipeline tests -- restores projected configuration state before endpoint assertions.

**Acceptance Criteria:**
- Given the current `Hexalith.Tenants.Aspire` package dependency graph, when `scripts/validate-nuget-packages.py` validates packed artifacts, then `ModelContextProtocol` is treated as an expected dependency and unrelated dependency drift still fails.
- Given sample event-pipeline tests create envelopes for tenant events, when the shared EventStore processor validates and dispatches them, then message-id validation passes and handlers update the in-memory projection.
- Given repeated domain event scenarios in sample tests, when the processor handles each envelope, then unique message IDs prevent accidental duplicate-marker classification while preserving domain-level repeated-event coverage.
- Given this fix is complete, when relevant CI-equivalent commands run locally, then package validation and `Hexalith.Tenants.Sample.Tests` pass without modifying submodule contents.

## Spec Change Log

## Verification

**Commands:**
- `dotnet test tests/Hexalith.Tenants.Contracts.Tests/Hexalith.Tenants.Contracts.Tests.csproj --configuration Release --filter "FullyQualifiedName~CiQualityGateScriptTests"` -- expected: package validator governance tests pass.
- `dotnet test samples/Hexalith.Tenants.Sample.Tests/Hexalith.Tenants.Sample.Tests.csproj --configuration Release` -- expected: sample endpoint and event-pipeline tests pass.
- `dotnet restore Hexalith.Tenants.slnx` -- expected: restore succeeds using the modern solution file.
- `dotnet build Hexalith.Tenants.slnx --configuration Release --no-restore` -- expected: Release build succeeds with zero warnings/errors.
- `rm -rf ./nupkgs && python3 scripts/pack-release-packages.py ./nupkgs 0.0.0-ci-test && python3 scripts/validate-nuget-packages.py ./nupkgs` -- expected: package set validates, including `Hexalith.Tenants.Aspire`.

## Suggested Review Order

**Package Boundary**

- Start with the CI failure gate and new expected dependency.
  [`validate-nuget-packages.py:94`](../../scripts/validate-nuget-packages.py#L94)

- Check the governance fixture mirrors the validator boundary.
  [`CiQualityGateScriptTests.cs:298`](../../tests/Hexalith.Tenants.Contracts.Tests/CiQualityGateScriptTests.cs#L298)

**Event Pipeline Tests**

- Verify access-check envelopes now satisfy EventStore message-id validation.
  [`AccessCheckEndpointsTests.cs:27`](../../samples/Hexalith.Tenants.Sample.Tests/Endpoints/AccessCheckEndpointsTests.cs#L27)

- Verify configuration envelopes use the same valid message-id helper.
  [`TenantConfigurationEndpointsTests.cs:26`](../../samples/Hexalith.Tenants.Sample.Tests/Endpoints/TenantConfigurationEndpointsTests.cs#L26)
