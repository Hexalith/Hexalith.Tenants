---
stepsCompleted:
  - step-01-document-discovery
  - step-02-prd-analysis
  - step-03-epic-coverage-validation
  - step-04-ux-alignment
includedDocuments:
  architecture:
    - _bmad-output/planning-artifacts/architecture.md
  epics:
    - _bmad-output/planning-artifacts/epics.md
  prd:
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/prd.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/addendum.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/.decision-log.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-a11y-l10n.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-audit-recovery.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-frontcomposer-depmap.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-operations-shell.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-phase-2-backlog.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-remove-user-journey.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-responsive-visual.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/reconcile-truth-state.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/review-adversarial.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/review-domain-fidelity.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/review-downstream-readiness.md
    - _bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/review-rubric.md
  ux:
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/DESIGN.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/EXPERIENCE.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/.decision-log.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/.working/prd-ux-digest.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/review-accessibility.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/review-rubric.md
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/mockups/mock-command-lifecycle.html
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/mockups/mock-consequence-preview.html
    - _bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/mockups/mock-tenant-list.html
---

# Implementation Readiness Assessment Report

**Date:** 2026-06-27
**Project:** tenants

## Document Discovery

### PRD Files Found

**Whole Documents:**
- None found

**Sharded Documents:**
- Folder: `_bmad-output/planning-artifacts/prds/prd-tenants-2026-06-02/`
  - `prd.md` (60,923 bytes, modified 2026-06-26 17:10)
  - `addendum.md` (15,527 bytes, modified 2026-06-26 17:10)
  - `.decision-log.md` (5,280 bytes, modified 2026-06-26 17:10)
  - `reconcile-a11y-l10n.md` (8,830 bytes, modified 2026-06-26 17:10)
  - `reconcile-audit-recovery.md` (11,272 bytes, modified 2026-06-26 17:10)
  - `reconcile-frontcomposer-depmap.md` (11,153 bytes, modified 2026-06-26 17:10)
  - `reconcile-operations-shell.md` (14,246 bytes, modified 2026-06-26 17:10)
  - `reconcile-phase-2-backlog.md` (16,179 bytes, modified 2026-06-26 17:10)
  - `reconcile-remove-user-journey.md` (12,635 bytes, modified 2026-06-26 17:10)
  - `reconcile-responsive-visual.md` (8,992 bytes, modified 2026-06-26 17:10)
  - `reconcile-truth-state.md` (14,210 bytes, modified 2026-06-26 17:10)
  - `review-adversarial.md` (25,286 bytes, modified 2026-06-26 17:10)
  - `review-domain-fidelity.md` (15,651 bytes, modified 2026-06-26 17:10)
  - `review-downstream-readiness.md` (18,304 bytes, modified 2026-06-26 17:10)
  - `review-rubric.md` (15,772 bytes, modified 2026-06-26 17:10)

### Architecture Files Found

**Whole Documents:**
- `_bmad-output/planning-artifacts/architecture.md` (58,352 bytes, modified 2026-06-26 17:10)

**Sharded Documents:**
- None found

### Epics & Stories Files Found

**Whole Documents:**
- `_bmad-output/planning-artifacts/epics.md` (114,166 bytes, modified 2026-06-26 17:10)

**Sharded Documents:**
- None found

### UX Design Files Found

**Whole Documents:**
- None found

**Sharded Documents:**
- Folder: `_bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/`
  - `DESIGN.md` (37,335 bytes, modified 2026-06-26 17:10)
  - `EXPERIENCE.md` (43,644 bytes, modified 2026-06-26 17:10)
  - `.decision-log.md` (15,133 bytes, modified 2026-06-26 17:10)
  - `.working/prd-ux-digest.md` (39,854 bytes, modified 2026-06-26 17:10)
  - `review-accessibility.md` (23,366 bytes, modified 2026-06-26 17:10)
  - `review-rubric.md` (14,148 bytes, modified 2026-06-26 17:10)
  - `mockups/mock-command-lifecycle.html` (20,692 bytes, modified 2026-06-26 17:10)
  - `mockups/mock-consequence-preview.html` (18,294 bytes, modified 2026-06-26 17:10)
  - `mockups/mock-tenant-list.html` (26,313 bytes, modified 2026-06-26 17:10)

### Discovery Issues

- No duplicate whole/sharded document formats found.
- Corrected discovery note: nested `prds/` and `ux-designs/` artifacts were found through the `epics.md` input-document inventory and confirmed for use.

## PRD Analysis

### Functional Requirements

FR1: A platform operator can browse and triage the tenant list by scanning, searching, filtering, sorting, and cursor-paging tenants. Each row must show tenant identity, status, member count, owner count, pending state, and a Truth State Badge with freshness. Loading, empty, filtered-empty, error, stale, and degraded states must stay distinct. Sorting or paging must never hide pending or stale markers. Search must match tenant Name or TenantId across the whole tenant set through server-side Memories syntactic/BM25 search, hydrate rows through the authoritative ETag/freshness read path, keep status filtering exact, tolerate search index lag, and fall back to the cursor list with a non-blocking notice when Memories is unavailable.

FR2: A user can open a tenant detail surface from the list and return with prior filter, sort, selection, and context preserved. Tenant detail must also support deep links.

FR3: A signed-in user can self-audit "My Tenants" by viewing the tenants they belong to and their role in each. Only memberships the caller is authorized to see are shown, with role and tenant status per row.

FR4: An operator can look up a user's memberships by search and can reach a user membership view from a member row. Results are authorization-scoped, and no visible memberships render as an explicit empty state rather than an error.

FR5: A user can view a tenant overview containing status, metadata, member summaries, configuration summaries, member counts, owner counts, lifecycle status, and a freshness indicator. Lifecycle/status must use no-color-only encoding.

FR6: A user can view tenant configuration key/values in read-only mode, grouped by namespace and filtered to namespaces they own or are authorized for. Values outside the caller's prefix are not shown, and sensitive-value display is outside the read MVP.

FR7: A user can copy a full identifier or support-safe reference even when visually truncated. Copied identifiers are literal caller-supplied strings, not assumed ULIDs or GUIDs, and copied values must never expose payloads, tokens, internal correlation ids, raw metadata, stack traces, or PII.

FR8: A user can review a tenant member table showing each member's role, owner count, status, freshness, and orphan or disabled context. The table is read-only, must not imply mutation, and must expose accessible table semantics including headers, sort state, and row relationships.

FR9: A user can see which member actions would be available and, when an action is unavailable, a plain-language inline Unavailable Action Reason using the six canonical categories. Reasons are not hover-only; tooltips may supplement but are never the only explanation.

FR10: An authorized user can add a user directly to a tenant by caller-supplied user id with an explicit role. There is no invitation or pending-member step. Adding an existing member is rejected as `UserAlreadyInTenant` and surfaced as safe localized text, not as a NoOp.

FR11: An authorized user can change a tenant member's role. Changing to the current role is a NoOp shown as `already applied`; role escalation and `TenantRole.Unknown` targets are rejected with safe localized text; success is shown only after projection confirmation.

FR12: An authorized user can remove a user from a tenant through fail-closed gating, Consequence Preview, elevated-friction handling, command lifecycle tracking, projection confirmation, and audit proof. Required inputs are validated before preview opens; the preview states owner-count impact, access revoked, recovery path, audit expectation, freshness, target platform standing, known consequences, and known unknowns. Last-owner removal is allowed with extra friction, target-also-global-admin raises platform friction without changing command scope, duplicate or already-applied removal is deduplicated, and unverifiable outcomes are never shown as success.

FR13: An authorized operator can create a tenant. Creating an existing tenant id is rejected as `TenantAlreadyExists`, and success is shown only after projection confirmation.

FR14: An authorized tenant contributor or global administrator can edit tenant metadata. Every successful edit emits `TenantUpdated` with no same-state suppression, and validation errors surface as safe localized field messages.

FR15: An authorized global administrator can disable or enable a tenant through a high-impact flow with Consequence Preview. Setting a tenant to an already-set lifecycle state is rejected as `TenantLifecycleStateAlreadySet`; disabled status is an eventually-consistent availability signal; commands targeting a disabled tenant are rejected as `TenantDisabled`; success is shown only after projection confirmation.

FR16: An authorized user can set a namespaced tenant configuration key/value. An identical key/value is a NoOp shown as `already applied`, values over domain limits are rejected as `ConfigurationLimitExceeded`, and configuration edits require Consequence Preview unless Product/UX later narrows the scope.

FR17: An authorized user can remove a tenant configuration key. Removing a missing key is rejected as `ConfigurationKeyNotFound`, and success is shown only after projection confirmation.

FR18: An authorized operator can review global administrators separately from tenant membership. The surface is hidden from tenant owners, reads the fixed-identity `global-administrators` aggregate, and shows identity plus freshness.

FR19: An authorized operator can grant or remove a global administrator except the last one. Removing the last global administrator is rejected as `LastGlobalAdministrator` and reflected as unavailable, not as completable friction; global administrator operations stay in the `global-administrators` scope and are never conflated with tenant membership.

FR20: A user can browse a tenant audit trail as a flat, stably ordered, cursor-paginated list with date and `AuditEventCategory` filters (`Access` and `Administrative`). Loading, empty, filtered-empty, and error states are distinct and accessible; the surface targets about 500 events without unacceptable degradation.

FR21: A user can reach audit evidence from navigation, tenant row, tenant detail, user lookup, and command result entry points, each scoped to the relevant tenant, user, or command.

FR22: A user can view a support-safe Audit Evidence Receipt for a recorded action, including actor, target, tenant scope, outcome, absolute timestamp, projection marker, and audit or command reference. It is assembled from structured narrative metadata, never from raw event payloads, and partial completion shows the actual lifecycle state rather than fabricated proof.

FR23: A user can distinguish `audit pending`, `audit delayed`, `audit unavailable`, and `missing implementation support`; none is shown as success, and each offers an appropriate retry, wait, continue-read-only, inspect-audit, or escalate path.

FR24: From audit evidence, an authorized user can start a compensating command such as "restore intended access" or "start correction". The correction is a new forward command with its own preview and proof, never called "undo", and the original event remains untouched. Tenant membership correction and global-administrator correction remain distinct command scopes.

FR25: A user can preview a correction against current state and link the original and corrective records. Success is shown only after projection confirmation.

Total FRs: 25

### Non-Functional Requirements

NFR1: Reads must use cursor pagination and conditional requests so unchanged data is cheap; freshness is surfaced, not hidden. Tenant list/detail/member surfaces target interactive rendering within about one second on a warm projection, and the audit view targets about 500 events without unacceptable latency.

NFR2: Authorization is server-enforced at the API and domain layers. The UI reflects authorization but never enforces it, must stay safe if it misjudges authorization, and role-scoped reads must prevent owners from seeing other tenants or global administrator surfaces.

NFR3: The UI must be correct under eventual consistency, at-least-once delivery, projection lag, and reconnects. The projection is the source of truth, commands and audit facts are confirmed by re-querying, and the UI never resurrects or displays optimistic success.

NFR4: Every interactive element and status must carry a stable automation selector or component contract, never keyed on row text or color, so acceptance and E2E tests are robust.

NFR5: The UI must never edit, delete, or rewrite events, projections, or state to fix data; corrections are forward compensating commands only.

NFR6: Accessibility must meet WCAG 2.1 AA, target WCAG 2.2 AA where the pinned Fluent UI Blazor and FrontComposer stack supports it, and include keyboard-only operation, complete-or-exit behavior, visible focus, focus return, modal focus traps, screen-reader semantics, no-color-only status, reduced-motion support, forced-colors support, and live-region correctness.

NFR7: Localization must use culture-aware, whole-string resources with named placeholders for state labels, role names, timestamps, warnings, unavailable reasons, recovery actions, confirmations, empty/loading/error/degraded/stale/unavailable copy, and rejection text. Runtime sentence-fragment assembly is forbidden.

NFR8: Support-safety and privacy are hard requirements: no surface, copy action, log, receipt, toast, or error may expose bearer tokens, decoded JWT contents, command payloads, serialized event bodies, raw EventStore metadata, internal correlation ids, stack traces, or real PII.

NFR9: Responsive behavior is desktop-first but must preserve safety at tablet and mobile widths. Safety-critical columns never drop, high-impact command flows are unavailable on mobile, and widths that cannot preserve full safety context fail closed with a visible reason.

NFR10: A UI story cannot be marked ready until applicable accessibility, localization, responsive, and documentation/reference evidence is cited, or a Product/UX-approved fallback records keyboard/focus/live-region behavior, copy ownership, documentation evidence, replacement path, and owner approval.

Total NFRs: 10

### Additional Requirements

- Build a new `src/Hexalith.Tenants.UI` Blazor InteractiveServer web host as the first implementation story, using .NET 10, `Microsoft.NET.Sdk.Web`, FrontComposer Shell, Fluent UI Blazor v5, and the existing Tenants AppHost.
- Use the InteractiveServer plus server-side BFF model. The browser must never call the backend directly and must never hold backend access tokens.
- Consume existing backend APIs only: `GET /api/tenants`, `GET /api/tenants/{tenantId}`, `GET /api/tenants/{tenantId}/users`, `GET /api/users/{userId}/tenants`, `GET /api/tenants/{tenantId}/audit`, `POST /api/v1/commands`, and `GET /api/v1/commands/status/{correlationId}`. Do not add backend endpoints for previews, receipts, or command status.
- Bind to `Hexalith.Tenants.Client` and `Hexalith.Tenants.Contracts` DTOs and enums. Do not redeclare DTOs, re-case wire fields, or parse `TenantId`/`UserId` as ULIDs or GUIDs.
- Implement typed BFF query and command gateways; all external backend egress goes through those gateways.
- Implement the command confirmation pattern: submit with a client-generated `messageId` ULID idempotency key, poll command status and listen for SignalR nudges concurrently, re-query the authoritative projection, and mark confirmed only from the re-queried projection.
- Treat SignalR projection notifications as freshness nudges only; they never advance command lifecycle or audit availability.
- Implement one shared Fluxor TruthState feature and casing-faithful vocabulary library for truth, freshness, command lifecycle, layered feedback, unavailable reasons, audit availability, and recovery verbs.
- Enforce non-collapse in state and reducers: `accepted`, `confirmed`, and `audit available` are distinct; `degraded` and `unable to verify` are success-prohibited; in-flight intent never overwrites last-confirmed projection data.
- Implement server-side conditional freshness reads, opaque/signed/scope-bound cursors, fail-closed authorization reflection, support-safe redaction/receipt/preview/rejection mapping, and Tenants-owned `.resx` resources under a `Tenants.` root.
- Use stable selectors in the form `data-testid="tenants-{surface}-{element}"` for all interactive controls and statuses.
- Organize the UI project by surface (`Components/Tenants`, `Components/Users`, `Components/GlobalAdministrators`, `Components/Audit`, `Components/Shared`) plus `State/`, `Services/`, `Vocabulary/`, `Resources/`, and `wwwroot/css/`.
- Add `tests/Hexalith.Tenants.UI.Tests` for bUnit/xUnit v3/Shouldly tests and a Playwright E2E tier for required acceptance scenarios.
- Keep missing shared UI capabilities in FrontComposer, not Tenants. Product/UX-approved fallbacks exist for flat audit DataGrid (`FC-AUD`), inline consequence text (`FC-CNS`), and one-at-a-time command policy (`FC-CNC`).
- FR15 disable/enable is reversible lifecycle soft-delete / availability control, not hard destructive tenant deletion. Hard destructive tenant deletion is out of scope and belongs to future administrators-only CLI tooling.
- Open questions remain for the command route alias, tenant-list grid decision (`FC-TBL`), localization ownership, WCAG 2.2 support, RTL scope, cursor invalidation behavior, config preview scope, audit-area MVP treatment, freshness thresholds, sensitive config values, source-spec ID correction, and owner self-service depth.

### PRD Completeness Assessment

The PRD is complete enough to extract a coherent 25-FR / 10-NFR requirements inventory and contains strong traceability scaffolding through UJ, CP, FR, NFR, SM, FC, and `ui-NN` identifiers. It also records significant implementation gates rather than hiding them: FrontComposer readiness, tenant-list grid caveats, command/audit fallback evidence, accessibility/localization evidence, and cursor/freshness decisions. Remaining risks for readiness are not missing requirements so much as unresolved gates and open decisions that must be tested against epics and stories.

## Epic Coverage Validation

### Epic FR Coverage Extracted

FR1: Covered in Epic 1 - Tenant workspace triage with searchable, sortable, cursor-paged tenant list and safe freshness/status visibility.
FR2: Covered in Epic 1 - Tenant detail navigation with preserved list context and deep-link support.
FR3: Covered in Epic 1 - "My Tenants" self-audit membership view.
FR4: Covered in Epic 1 - User membership lookup with authorization-scoped results.
FR5: Covered in Epic 1 - Tenant overview with status, metadata, counts, lifecycle, and freshness.
FR6: Covered in Epic 1 - Read-only namespaced tenant configuration visibility.
FR7: Covered in Epic 1 - Support-safe copy behavior for identifiers and references.
FR8: Covered in Epic 1 - Read-only tenant member table with accessible table semantics.
FR9: Covered in Epic 1 - Action availability and inline unavailable-action reasons.
FR10: Covered in Epic 2 - Add user to tenant with explicit role and safe rejection handling.
FR11: Covered in Epic 2 - Change tenant member role with NoOp and rejection handling.
FR12: Covered in Epic 2 - Remove tenant member with fail-closed gating, preview, lifecycle tracking, and proof.
FR13: Covered in Epic 2 - Create tenant with projection-confirmed success.
FR14: Covered in Epic 2 - Edit tenant metadata with safe validation handling.
FR15: Covered in Epic 3 - Disable or enable tenant with high-impact controls and projection confirmation.
FR16: Covered in Epic 3 - Set tenant configuration key/value with preview and NoOp/rejection handling.
FR17: Covered in Epic 3 - Remove tenant configuration key with projection-confirmed outcome.
FR18: Covered in Epic 4 - Review global administrators separately from tenant membership.
FR19: Covered in Epic 4 - Grant or remove global administrators with last-global-admin protection.
FR20: Covered in Epic 5 - Browse flat, filtered, cursor-paginated tenant audit trail.
FR21: Covered in Epic 5 - Reach audit evidence from navigation, rows, detail, lookup, and command results.
FR22: Covered in Epic 5 - View support-safe Audit Evidence Receipt assembled from structured narrative data.
FR23: Covered in Epic 5 - Distinguish audit pending, delayed, unavailable, and missing-support states.
FR24: Covered in Epic 5 - Start forward compensating commands from audit evidence.
FR25: Covered in Epic 5 - Preview correction against current state and link original/corrective records.

Total FRs in epics: 25

### Coverage Matrix

| FR Number | PRD Requirement | Epic Coverage | Status |
| --- | --- | --- | --- |
| FR1 | Tenant list browse/search/filter/sort/page with required columns, truth/freshness states, authorization safety, and Memories-backed whole-set search behavior. | Epic 1, Story 1.2 | Partial: ID covered; Memories/BM25 search consequences are not reflected in the epics/story text. |
| FR2 | Open tenant detail and return with context preserved; support deep links. | Epic 1, Story 1.3 | Covered |
| FR3 | Self-audit My Tenants with authorized memberships, roles, and tenant status. | Epic 1, Story 1.4 | Covered |
| FR4 | Look up a user's memberships with authorization-scoped empty state. | Epic 1, Story 1.5 | Covered |
| FR5 | View tenant overview with status, metadata, counts, lifecycle, and freshness. | Epic 1, Story 1.3 | Covered |
| FR6 | View read-only namespaced tenant configuration within authorized prefixes. | Epic 1, Story 1.6 | Covered |
| FR7 | Copy support-safe identifiers/references without leaking unsafe data. | Epic 1, Story 1.8 | Covered |
| FR8 | Review read-only tenant member table with accessible semantics. | Epic 1, Story 1.7 | Covered |
| FR9 | Show action availability and inline unavailable-action reasons. | Epic 1, Story 1.7 | Covered |
| FR10 | Add user to tenant with explicit role and safe rejection handling. | Epic 2, Story 2.2 | Covered |
| FR11 | Change member role with NoOp/rejection handling and projection-confirmed success. | Epic 2, Story 2.3 | Covered |
| FR12 | Remove member with fail-closed gating, preview, lifecycle tracking, proof, and no false success. | Epic 2, Story 2.4 | Covered |
| FR13 | Create tenant with existing-id rejection and projection-confirmed success. | Epic 2, Story 2.1 | Covered |
| FR14 | Edit metadata with always-emitted update and safe validation handling. | Epic 2, Story 2.5 | Covered |
| FR15 | Disable/enable tenant as high-impact global-admin-only flow with projection confirmation. | Epic 3, Stories 3.1 and 3.2 | Covered |
| FR16 | Set tenant configuration with NoOp/rejection handling and preview. | Epic 3, Story 3.3 | Covered |
| FR17 | Remove tenant configuration key with missing-key rejection and projection confirmation. | Epic 3, Story 3.4 | Covered |
| FR18 | Review global administrators separately from tenant membership. | Epic 4, Stories 4.1 and 4.2 | Covered |
| FR19 | Grant/remove global administrators with last-admin hard stop. | Epic 4, Stories 4.3 and 4.4 | Covered |
| FR20 | Browse flat, filtered, cursor-paginated tenant audit trail. | Epic 5, Story 5.1 | Covered |
| FR21 | Reach audit evidence from navigation, rows, detail, lookup, and command results. | Epic 5, Story 5.2 | Covered |
| FR22 | View support-safe Audit Evidence Receipt. | Epic 5, Story 5.3 | Covered |
| FR23 | Distinguish audit pending/delayed/unavailable/missing-support states with recovery. | Epic 5, Stories 5.2, 5.3, and 5.4 | Covered |
| FR24 | Start forward compensating commands from audit evidence. | Epic 5, Story 5.5 | Covered |
| FR25 | Preview correction against current state and link original/corrective records. | Epic 5, Story 5.6 | Covered |

### Missing Requirements

No PRD FR number is completely missing from the epic coverage map. The epics cover FR1 through FR25 by identifier.

### Partial Coverage / Detail Gaps

FR1: The PRD's post-2026-06-21 search consequences are not fully carried into the epics document. Specifically, the epics do not mention `Hexalith.Memories`, syntactic/BM25 search, matching by Name or TenantId across the whole tenant set, server round-trip on non-empty search, authoritative ETag/freshness hydration of search hits, exact status filtering, search index lag behavior, or fallback to the cursor list when Memories is unavailable.

### Coverage Statistics

- Total PRD FRs: 25
- FRs covered in epics by identifier: 25
- ID-level coverage percentage: 100%
- FRs with partial detail coverage: 1
- Detail-level complete FRs: 24 of 25

## UX Alignment Assessment

### UX Document Status

Found. UX documentation exists under `_bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/` and includes final UX spines (`DESIGN.md`, `EXPERIENCE.md`), decision log, PRD digest, accessibility/rubric reviews, and three illustrative HTML mockups.

### UX to PRD Alignment

The UX spines align strongly with the PRD's core requirements:

- UX surfaces map to PRD FRs: tenant list, tenant detail, My Tenants, user lookup, member table, global administrators, audit trail, audit evidence receipt, consequence preview, command lifecycle, and command/correction flows.
- UX journeys preserve the PRD UJ model: Elena triage/access/change/onboarding, Sofia audit/recovery, Nadia owner self-service, and Marc self-audit.
- UX preserves the PRD truth contract: projection-confirmed success only, SignalR as nudge only, non-collapse of accepted/confirmed/audit-available, fail-closed gating, forward corrections only, and support-safe receipts.
- UX preserves accessibility/localization requirements: WCAG 2.1 AA baseline, conditional WCAG 2.2, keyboard complete-or-exit, focus trap/return, forced-colors/no-color-only, live-region politeness, absolute timestamps, whole-string localization, and evidence gates.
- UX resolves or incorporates several PRD/reconcile issues: Users is contextual rather than primary nav, config edits always require preview, audit MVP is an honest not-yet-available state, RTL is layout-ready but not v1-verified, and Product/UX fallbacks exist for flat audit, inline consequence text, and one-at-a-time commands.

### UX to Architecture Alignment

Architecture supports the UX direction in the main areas:

- Frontend stack: new `Hexalith.Tenants.UI` Blazor InteractiveServer host using FrontComposer Shell and Fluent UI Blazor v5.
- Safety boundary: server-side BFF means browser does not call backend directly and does not hold backend tokens.
- State model: shared Fluxor TruthState feature plus `Vocabulary/` library supports casing-faithful canonical tokens and non-collapse.
- Data/freshness: server-side query gateway, ETag/304 conditional reads, opaque cursors, server-held cursor handling, SignalR nudge-only path, and projection re-query confirmation.
- Authorization/support-safety: server-side authorization reflection, redaction, receipt/preview/rejection mapping, and Tenants-owned `.resx` resources.
- Components/structure: the architecture gives homes for the ten UX domain components and maps all 25 FRs to component folders, services, state, vocabulary, and tests.
- Verification: bUnit, Playwright, stable `data-testid` selectors, six list states, required acceptance scenarios, and a11y/live-region/focus/forced-colors evidence are represented.

### Alignment Issues

1. **FR1 tenant search detail is not fully carried through UX/epic story text.** PRD and architecture now specify Memories-backed whole-set search, search-hit hydration through authoritative ETag/freshness reads, search-index lag behavior, exact status filtering, and fallback to the cursor list when Memories is unavailable. UX covers search as a tenant-list behavior but does not explicitly define the Memories-unavailable notice or search-specific degraded state/copy.

2. **Freshness `aging` has an architecture limitation.** UX defines `aging` as a distinct usable-with-friction freshness state. Architecture D6 states the current wire can only produce `current`/`stale`/`unknown`, with `aging` collapsed into `current` until a projected timestamp wire field exists. This is an honest technical constraint but a UX/architecture gap for the full five-state freshness model.

3. **Render-mode documentation is reconciled but should remain synchronized.** UX originally referenced a Blazor Auto lifecycle assumption; architecture supersedes it with InteractiveServer plus server-side BFF. The architecture records this as a divergence with the same consistency invariants preserved. Any downstream story text should use InteractiveServer to avoid reintroducing Auto assumptions.

4. **FC-TBL readiness/status needs synchronized wording.** UX states the Story 1.2 `FC-TBL` tenant-list decision still applies; architecture says Story 1.2 resolved the path with a Tenants-specific `TenantDataGrid` while reusable grid capability remains a FrontComposer concern. Epics still phrase Story 1.2 as a pre-build gate. Sprint status/story handoff should make the current decision state explicit.

5. **Mockups are illustrative and conflict with implementation rules if copied literally.** The HTML mockups use standalone raw HTML/CSS and placeholder colors/glyphs by design. They explicitly say spines win on conflict. Implementation must follow Hexalith UX rules: FrontComposer/Fluent v5 components first, no raw controls when Fluent/FrontComposer equivalents exist, no theme redefinition, Fluent tokens/parameters for typography/color/spacing, and accordions for multi-section page/dialog surfaces.

### Warnings

- Story-specific accessibility, localization, responsive, documentation/reference, forced-colors, live-region, and focus evidence remains required. The presence of UX spines does not satisfy story ready gates by itself.
- Fluent UI v5 is pinned as an RC; exact token, ARIA, MessageBar live-region, icon, contrast, and forced-colors behavior must be verified against the pinned package at build.
- `FC-TOK` remains a missing shared capability. Tenants can proceed with canonical vocabulary plus verified Fluent semantic/icon mapping, but a shared token contract remains future work.
- Missing shared UI capabilities must stay in FrontComposer or approved fallbacks; Tenants should not absorb generic UI scaffolding.
