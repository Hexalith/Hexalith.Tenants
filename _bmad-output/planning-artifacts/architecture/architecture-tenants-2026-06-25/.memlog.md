---
scope: Hexalith.Tenants domain module architecture spine
purpose: build-substrate
altitude: feature
updated: 2026-06-29T14:56
---

- (direction) User selected Coaching path for architecture spine creation.
- (assumption) User response 'ok' interpreted as spine-only deliverable for now; no additional human-facing artifact requested.
- (event) resumed architecture spine run from existing memlog; prior rendered spine was still the untouched template
- (constraint) Existing architecture.md, PRD, UX, epics, implementation artifacts, and current code are binding reality checks for the spine; current code already contains src/Hexalith.Tenants.UI and tests/Hexalith.Tenants.UI.Tests
- (constraint) UI runtime is Blazor InteractiveServer with a server-side BFF; browser never calls Tenants/EventStore APIs directly and never holds backend access tokens
- (constraint) Tenants UI composes Hexalith.FrontComposer and Fluent UI Blazor v5; missing generic UI capability belongs in FrontComposer or an approved fallback, not Tenants
- (constraint) Projection re-query is the source of UI confirmation; command accepted, projection confirmed, and audit available remain non-collapsed states
- (constraint) Tenant reads go through direct Tenants REST endpoints and in-process query handlers; do not route reads through the EventStore generic query gateway or a retired projection actor
- (decision) Paradigm selected by user: FrontComposer-composed domain UI. Binds Tenants Management UI surfaces. Prevents treating Tenants UI as an independent bespoke Blazor app or as a backend architecture spine. Rule: the spine centers shell/module composition, page-local surface composition, and domain UI boundaries; event-sourced backend/BFF constraints are inherited context where they affect composition.
- (decision) Tenants contributes exactly one FrontComposer shell navigation entry for the module and keeps Tenants-domain sub-surfaces inside the workspace as page-local tabs/contextual links. Binds FR1-FR4, FR18-FR21, Operations Shell IA. Prevents left-nav proliferation and incomplete all-users inventory claims. Rule: /tenants is the shell entry; Users is lookup-backed, not exhaustive; Global Administrators and Audit stay internal/contextual unless a future IA decision changes that.
- (decision) FrontComposer/Fluent components are the first composition surface. Binds all module UI components and UX-DR1..UX-DR33. Prevents raw interactive HTML, local shell scaffolding, duplicate theme definitions, and generic component infrastructure in Tenants. Rule: use FrontComposer or Fluent UI Blazor V5 equivalents first; custom markup/CSS is allowed only for domain-specific gaps or documented Fluent/FrontComposer absences.
- (decision) Tenants owns domain-specific composition components and copy, while FrontComposer owns reusable shell/layout/component capabilities. Binds components under src/Hexalith.Tenants.UI/Components and resources. Prevents moving generic tabs, grids, page chrome, or theme primitives into Tenants, and prevents moving Tenants domain vocabulary into shell resources. Rule: Tenants owns domain surfaces, domain state labels, support-safe copy, and BFF composition; reusable UI primitives become FrontComposer work or approved fallbacks.
- (decision) The server-side BFF gateways are the only backend egress from the UI host. Binds Services/Gateways, command flows, query flows, auth/token handling. Prevents browser-side backend calls, token exposure, component-to-backend coupling, and duplicated transport paths. Rule: components dispatch or call composed panels; effects/services use ITenantQueryGateway/ITenantCommandGateway; tokens and DAPR/service invocation stay server-side.
- (decision) Projection-confirmed truth is a composition invariant, not per-surface policy. Binds truth badges, list/detail/member/audit/global-admin surfaces, command flows. Prevents optimistic success and accepted/confirmed/audit-available collapse. Rule: every surface renders freshness, command, projection, authorization, and audit states from shared typed state; SignalR and Memories are nudges/indexes only, never proof.
- (decision) Tenant reads use direct Tenants REST endpoints and in-process query handlers, not the EventStore generic query gateway. Binds ITenantQueryGateway, TenantsQueryController, all read surfaces. Prevents losing projection ETags/freshness and resurrecting retired projection-actor routing. Rule: read composition calls GET /api/tenants* and /api/users/{userId}/tenants through the server-side BFF.
- (decision) Freshness uses EventStore read-model freshness metadata adopted by Tenants. Binds list/detail/user/global-admin/audit surfaces and high-impact action availability. Prevents Tenants-owned duplicate freshness enums and treating 304, ServedAt, or search results as proof of current data. Rule: use ReadModelFreshnessState; Refreshing is a UI transient; stale/unknown fail closed where safety requires it.
- (decision) Support safety and localization are server-side/domain-owned composition concerns. Binds copy actions, receipts, previews, rejection text, resources. Prevents tokens, payloads, correlation ids, ETags/cursors, raw metadata, stack traces, or PII from entering rendered output; prevents shell-owned domain copy. Rule: TenantsResources whole-string EN/FR resources and BFF redaction/receipt/preview assembly own all domain user-facing text.
- (decision) Memories is search-as-index-only for tenant list discovery. Binds FR1 cross-set search. Prevents adding a Tenants/EventStore list-filter endpoint, using Memories as row truth, or blocking the list on search outage. Rule: Memories returns tenant ids from tenants-index; the BFF hydrates rows through the authoritative Tenants detail/read path and degrades to cursor list when unavailable.
- (decision) UI conformance tests are architectural guardrails. Binds tests/Hexalith.Tenants.UI.Tests and route smoke tests. Prevents silent drift from Fluent/FrontComposer composition, localization parity, support safety, selector stability, and route IA. Rule: add or update bUnit/conformance tests with each surface change; do not loosen guards without an explicit approved story.
- (version) Stack reality checked from global.json and Directory.Packages.props: .NET SDK 10.0.301, Dapr 1.18.4, Aspire 13.4.6, Microsoft.FluentUI.AspNetCore.Components 5.0.0-rc.3-26138.1, Hexalith.EventStore 3.19.0, Hexalith.Memories 1.31.1, xUnit v3 3.2.2, bUnit 2.8.4-preview.
- (decision) Command flows use the shared FrontComposer command posture and approved one-at-a-time fallback. Binds FR10-FR17, FR19, FR24-FR25. Prevents independent command components choosing optimistic success, concurrent submits, bulk action, toast batching, or bypassing preview/gating. Rule: command UX composes the shared gateway/lifecycle pattern, consequence preview where required, projection re-query confirmation, and one-at-a-time concurrency until a stronger FrontComposer contract replaces the fallback.
- (decision) The Tenants UI host is a presentation host owned by this domain repo and orchestrated by the repo AppHost. Binds deployment, local run, containerization, and auth/service reference wiring. Prevents moving domain UI into FrontComposer, making it a NuGet package, adding Dockerfiles, or duplicating shared ServiceDefaults. Rule: src/Hexalith.Tenants.UI publishes a tenants-ui SDK container and is wired by Hexalith.Tenants.AppHost; shared platform hosting stays in shared modules.
- (event) spine finalized after lint and sequential reviewer-gate fallback reviews
