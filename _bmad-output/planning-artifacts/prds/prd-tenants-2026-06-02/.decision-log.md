# PRD Decision Log — Tenants Management UI

Canonical memory and audit trail for this PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

- **Run:** prd-tenants-2026-06-02
- **Intent:** Create
- **Subject:** End-user UI for managing tenants, built on Hexalith.FrontComposer.

## Decisions

### 2026-06-02 — Run initialized
- Intent: **Create** — no prior PRD existed under `_bmad-output/planning-artifacts/`.
- Subject confirmed by user: *"Build the end user UI for managing tenants using FrontComposer."*
- Grounding: extracted 8 existing `docs/tenants-ui-*` Epic 9 readiness specs via subagents + ran landscape research on comparable tenant/member-management admin UIs (Auth0, GitHub, AWS, Stripe, Entra, GitLab, Slack, Atlassian).
- Finding: existing specs are detailed but **planning-only / dependency-tracking** (written in `FC-*`/`blockedBy` vocabulary); there is **no product-altitude PRD above them**. The PRD's role is to consolidate them to product altitude (Vision, Users/JTBD, Journeys, Glossary, Features/FRs, Scope, Metrics, Non-Goals).
- Finding: several high-value flows are **blocked on missing FrontComposer components** — `<AuditTimeline>` (`FC-AUD`), `<ConsequencePreview>` (`FC-CNS`), unconfirmed layout contract (`FC-LYT`), missing tokens (`FC-TOK`). This narrows "buildable now" vs. full vision and drives the scope decision.

### 2026-06-02 — Calibration resolved (user)
- **Audience:** **Both — one app, role-scoped** (platform operators/global admins + tenant owners; authorization-scoped, server-enforced).
- **PRD scope:** **Full Phase 2 vision (`ui-01..15`), MVP flagged inside.**
- **Working mode:** **Fast path** — draft full PRD with `[ASSUMPTION]` tags, then iterate.
- **Primary goals (all four selected):** safe operations without raw API; trustworthy audit & recovery; reduce operator load via self-service; onboard & scale tenants faster.
- **MVP boundary:** **Read-only foundation only** (`ui-01..06` equivalent). Command flows → Phase 2b (unblocked) and 2c (high-impact/audit/recovery).

### 2026-06-02 — Assumptions baked into draft (user-flagged, awaiting confirmation at Finalize)
- No email-invitation flow — direct add by user id (backend has no invitation concept).
- MVP uses approved FrontComposer fallbacks (flat audit list, inline consequence text) where rich components are missing; rich components are a FrontComposer dependency, not built in Tenants.
- Authorization server-enforced; UI reflects only.
- Desktop-first; mobile read-only (no high-impact commands on mobile).
- Full assumption set indexed in `prd.md` §17.

### 2026-06-02 — Full draft produced
- `prd.md` written: 17 sections, 9 features, FR-1..FR-25, UJ-1..UJ-6, CP-1..CP-9, NFR-1..5, SM-1..5 + 3 counter-metrics, 11 open questions.
- `addendum.md` written: FR→backlog→spec map, FrontComposer dependency readiness table, backend surfaces, mechanism rationale, options-considered.

### 2026-06-02 — Finalize executed
- **Decision-log audit:** clean; all logged decisions reflected in PRD/addendum.
- **Input reconciliation:** 8 subagents (one per source spec) vs PRD+addendum → `reconcile-*.md`. Folded fixes: "approved"→"proposed" fallback everywhere; FC-CNC corrected to *missing*; §11 readiness aligned with addendum; Phase 2b reframed (no unblocked command flows); `aging`/`degraded`/`unable to verify` restored; target-also-global-admin friction added; dropped list states/columns + breakpoints + a11y ready-gate restored; FR-22/24/25 flagged as un-backed; ID-scheme spec error caught (PRD correct, specs wrong).
- **Reviewer gate:** 4 parallel reviewers → `review-rubric.md` (strong/disciplined), `review-domain-fidelity.md` (code-verified: found CP-6 last-global-admin error [CRITICAL], FR-10 add-NoOp error, FR-14 always-emits, disable/enable rejection rules), `review-downstream-readiness.md` (state-name drift), `review-adversarial.md` (build-readiness oversold; both-audiences thin; metrics measure activity not thesis).
- **Triage:** user decision — **owner self-service = honest-minimal scoping** (shared surfaces, no owner-only screens v1; flagged as downstream-UX gap). All other findings auto-fixed (correctness/honesty/consistency).
- **Fixes applied (rewrite):** domain-fidelity (CP-6 asymmetry, FR-10/14/15/19, rejection/NoOp matrix in addendum §D); build-readiness honesty (§2, §14 status banner, §14.3); canonical state sets enumerated verbatim (addendum §G) + notation normalized; trust metric SM-6 added; SM-1 FR range fixed; phase labels standardized; "global administrator" naming; pending-state + Users-area↔FR; read-surface perf bound (NFR-1).
- **Polish:** editorial structural+prose tightening applied inline during the rewrite (formal `bmad-editorial-review-structure`/`-prose` offered as optional follow-up).
- **Deferred (open items, owners):** §16.1 route (eng), §16.2/§16.3 FrontComposer+layout = critical path (FrontComposer + Product/UX), §16.4-11 (Product/UX/eng), §16.12 spec ID correction (spec owners), §16.13 owner self-service depth (Product). Metric targets placeholders (PM).
- **Close:** `prd.md` frontmatter set `status: final`, `updated: 2026-06-02`. `finalize_reviewers`/`external_handoffs`/`on_complete` empty → skipped.
