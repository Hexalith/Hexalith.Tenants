namespace Hexalith.Tenants.UI.State.GlobalAdministrators;

public enum GlobalAdministratorsSurfaceKind {
    Loading,
    Ready,
    Empty,
    Unknown,
    Stale,
    Degraded,
    Error,
    Unauthorized,
    Unavailable,
    Invalid,
}
