@inherits LayoutComponentBase
@using Hexalith.FrontComposer.Shell.Components.Layout

@* The shell is fully framework-owned: it renders the left navigation from the domain's registered
   nav entries (TenantsFrontComposerRegistration) and the header account control (FcAccountMenu),
   which owns sign in / sign out. The Tenants UI therefore only composes its body through the shell. *@
<FrontComposerShell>@Body</FrontComposerShell>
