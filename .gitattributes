# Auto-detect text files and normalize line endings
* text=auto

# Force LF for shell scripts (must not have CRLF on Unix)
*.sh text eol=lf

# Force LF for other common text formats
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
